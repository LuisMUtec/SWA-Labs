# Iteración 13 — 2026-08-27

Evaluado: [`redale/R-requerimientos/backlog.md`](../../redale/R-requerimientos/backlog.md)
@ `13b1fdb` — **174 RF + 18 RNF = 192 ítems**.

Rúbrica: [`evals/README.md`](../README.md), con `D4` en su forma enmendada del 2026-08-27.

## Puntaje

| Dim | Pts | Obtenido | Por qué |
|---|---|---|---|
| D1 Satisfacción de las personas | 3 | **1,5** | Los tres devuelven `Funciona con reservas` por cuarta ronda consecutiva |
| D2 Ajuste al problema | 3 | **3,0** | Censo **102 de 192 (53,1 %)**: frontera 15 · moneda 8 · efectivo/tercero 71 |
| D3 Cobertura de seguridad y consistencia | 2 | **1,5** | Consistencia 1,0. Seguridad 0,5: el camino de la ronda 12 **está cerrado**; hay uno nuevo |
| D4 Coherencia del backlog | 2 | **1,66** | `n = 13` · `d = 13/192 = 6,77 %` · `2 × (1 − 0,0677/0,40)`. Ningún techo duro ata |
| **Total** | **10** | **7,66** | |

**Gate ≥ 8: NO PASA**, por 0,34. Decimotercera ronda, decimotercera vez.

**Contra la 12 (7,34): +0,32 — y el total entero lo aporta D4.** `D1`, `D2` y `D3` no se movieron
un punto. `n` cayó de 23 a **13** mientras el backlog crecía 9,7 %, de modo que la densidad quedó en
**6,77 %**: la mitad del récord anterior y un quinto del 36,6 % de la ronda 01.

## Lo que hay que decir antes que nada

**Los cuatro bloques que separaban del gate se escribieron los cuatro, cerraron las tres reservas
nombradas, y D1 no se movió.** Cada persona encontró una reserva nueva **de la misma clase** que la
que se le acababa de cerrar:

| Persona | Reserva cerrada esta ronda | Reserva nueva |
|---|---|---|
| Rosa | `RF-149` sin aviso → `RF-172` | La palabra «constancia» de la que `RF-149` depende **no tiene productor** |
| Elena | La verificación del código → `RF-178`/`RF-179`/`RF-180` | `RF-172` **viaja a una sola punta** y falta la suya |
| Kevin | El motivo de la retención → `RF-170`/`RF-171` | `RNF-09` fija 10 minutos y **no dice qué pasa al vencerlos** |

**Es la sexta ronda consecutiva del patrón.** Y esta vez con una forma más precisa: las tres reservas
nuevas son *«el título existe y le falta el par»*, no *«no existe el título»*. El escalón de D1 es
medio punto y no distingue entre las dos cosas.

## Veredictos de persona

### Rosa — `Funciona con reservas` (resta 0,5)

- **Flujo y ramo malo:** enteros. Reconoce el bloque 1 con nombre propio: *«el backlog calla el
  motivo (RF-170, RF-171) sin callar el plazo»*. Cero deducción por permisos, verificada uno por uno.
- **Qué la frustra:** **(a) `RF-149` gira sobre la palabra «constancia» y ningún título dice qué la
  produce.** `RF-131` la registra *«cuando el canal lo reporta»*; su teléfono es prepago. Puede salir
  de la ventanilla con el mensaje leído en la mano y el sistema afirmando que no llegó — `RF-173`
  solo revierte «cuando la constancia llega», y si el canal nunca reporta, nunca llega. **(b) `RF-154`
  decide si la reposición de su madre procede y no tiene plazo ni terminador, mientras `RF-153` le da
  cinco días hábiles a la objeción del agente y `RF-184` le pone final.** *«El backlog le puso plazo a
  la disputa del agente y se lo dejó abierto a la reposición de mi madre.»*
- **Anota sin restar:** `RF-187` le presenta la solicitud del receptor *«como si el sistema la hubiera
  originado»*, ocultándole el origen — que es justo el dato con el que desconfiaría si alguien se
  hiciera pasar por su madre.

### Elena — `Funciona con reservas` (resta 0,5)

- **Flujo principal:** los cinco pasos y los cuatro ramos tienen ítem, y **sus tres desastres reales
  están cerrados**: `RF-122` el documento vencido, `RF-154`+`RF-143` la diferencia con responsable
  que no es el bodeguero, `RF-64` el cierre en su mano.
- **Qué la frustra:** **`RF-172` avisa el paso a *no pagable* solo al emisor.** Ella ya recibió
  `RF-25` y `RF-41`; toma la combi con esos dos avisos vivos y el giro no pagable. Lo apoya en la
  simetría del propio backlog —`RF-51`/`RF-120`, `RF-67`/`RF-37`, `RF-60`/`RF-121`,
  `RF-14`/`RF-156`—: **`RF-172` es el único cambio de estado que viaja a una sola punta.** Segundo
  punto: `RF-75` y `RF-109` no distinguen si «puede pagarle» habla de la caja del punto o de la
  disponibilidad del giro.
- **Comprobado y no deducido:** cero por permisos, verificado ítem por ítem, incluido `RF-187`, que
  la deja *solicitar* y no cambiar.

### Kevin — `Funciona con reservas` (resta 0,5)

- **Flujo y ramos:** enteros, con los dos críticos en formulación fuerte —`RF-118` nombra el efectivo
  cobrado sin giro creado; `RNF-05` hace el doble pago imposible *«en ningún punto y en ningún
  momento»*— y `RF-13` con el momento nombrado. **Su reserva de la ronda 12 está cerrada:** cita
  `RF-40`+`RF-170` y `RF-152`+`RF-171` como cubiertos.
- **Qué lo frustra:** **`RNF-09` fija 10 minutos de techo a la espera del segundo rol y no dice qué
  pasa al vencerlos**, *«cuando RF-177 y RF-184 demuestran que este backlog sabe escribir ese
  final»*. A los diez minutos y un segundo, con la persona delante, ningún título dice si el giro
  queda rechazado ni qué se le ofrece.
- **Anota sin restar:** el giro que no puede pagar por caja insuficiente no tiene estado nombrado;
  `RF-144` es el único estado de corte que no dice «que el operario puede leer»; y **`RF-184` le
  cobra a su caja el silencio de un rol ajeno**.
- **Comprobado y no deducido:** *«lo busqué activamente, que es donde suele estar el defecto»* —
  ningún título le da libertad que lo convierta en el eslabón comprable.

`D1 = 3 − 0,5 − 0,5 − 0,5 = **1,5**`

## D2 — censo · 3,0

| Rasgo | Ítems | Pts |
|---|---|---|
| Cruza una frontera | 15, con **`RF-185`** nuevo — *«a cada autoridad dentro del plazo que fija esa jurisdicción, y no del más largo de los dos»* es falso si hay una sola autoridad | **1** |
| Cruza una moneda | 8. **Ninguno de los 18 ítems nuevos toca este rasgo** — es el más delgado del backlog desde la ronda 04 | **1** |
| Termina en efectivo, sin banco, en manos de un tercero | 71, con `RF-170`, `RF-177`, `RF-180`, `RF-183`, `RF-187` nuevos | **1** |

**102 de 192 (53,1 %).** Serie: `4/41` → `41/81` → `51/97` → `73/110` → `78/118` → `99/137` →
`95/155` → `96/175` → **`102/192`**.

**La fracción bajó por tercera ronda consecutiva**, pero **el ritmo cayó a un cuarto**: −1,8 puntos
contra −6,4. La ronda 12 condicionó la pérdida a *«una ronda más al mismo ritmo»*, y el ritmo no se
mantuvo. La regla operativa de la rúbrica es existencial y por rasgo, y los tres la superan con
holgura. **D2 = 3,0.**

**El aviso se agrava, no se levanta, y por dos razones:**

1. **El bloque 8 es el único de los ocho que no se aplicó en absoluto.** Pedía que cada título nuevo
   muriera en una transferencia doméstica o llevara la razón escrita de por qué no. **Doce de
   dieciocho sobreviven y ninguno lleva razón.** La métrica propia del bloque —mortalidad de los
   títulos nuevos— **empeoró de 54 % a 33 %**. La fracción global cayó menos solo porque se agregaron
   menos ítems.
2. **El recuento independiente del agregador, enumerado ID por ID, da 49,0 % — por debajo de la
   mitad.** La ronda 12 publicó los IDs de frontera y de moneda pero no los 74 del rasgo 3, de modo
   que su 54,9 % no era auditable. El agregador **no cruzó el umbral con un criterio nuevo** —cambiar
   la vara a mitad de serie es lo que el Principio IV prohíbe— y publicó el censo entero para que la
   ronda 14 sí lo sea.

**Y deja anotado un candidato a enmienda, sin aplicarlo:** D2 lleva **diez rondas consecutivas
leyendo 3,0** (04 a 13) mientras la fracción se movió entre 52,6 % y 61,3 % y la disciplina de
escritura se degradó de forma medible. Un test existencial por rasgo no distingue un backlog que
ataca la remesa de uno que la ataca en seis ítems de 192. **Es la misma evidencia —varianza baja
frente a un sustrato que sí varía— que forzó la enmienda de D4 en la ronda 07.**

## D3 — seguridad y consistencia · 1,5

### Consistencia · 1,0

Seis invariantes declarables, cero adjetivos. Los tres lugares cubiertos, y **`RF-184` cierra la
última puerta batiente de la frontera contable con el agente** — era el terminador que la ronda 12
dejó abierto.

**Se anota sin deducir** la carrera `RF-173` ↔ `RF-174`: `RF-174` devuelve a las 72 h y `RF-173`
vuelve pagable *«cuando la constancia llega»*, sin guarda contra un giro ya devuelto. `RNF-13`
prohíbe *devuelto y cobrado*, no *devuelto y pagable*. **Es el candidato a deducción de la ronda 14
si no se ordena.**

### Seguridad · 0,5 — el camino de la ronda 12 cerrado, uno nuevo abierto

| Pedido en el bloque 1 | Estado |
|---|---|
| Quién no puede leer el motivo de una **retención** | **`RF-170`, P0.** Cerrado |
| Que la acción del giro **retenido** no permita deducirlo | **`RF-171`, P0.** Cerrado |
| Corregir la afirmación de `L` apoyada en `RF-40` | **No aplicado** — segunda ronda |

**El medio punto nuevo cae entre las tres personas, y por eso ninguna podía verlo:**

> `RF-187` —*admitirá del receptor la solicitud de cambio de punto, y la ofrecerá al emisor como si
> el sistema la hubiera originado*— es el **tercer acto remoto originado por el receptor y el único
> sin compuerta de identidad**. `RF-126` identifica antes de admitir una declaración de diferencia;
> `RF-180` identifica antes de responder una consulta. **Cambiar dónde se paga el dinero es la más
> consecuente de las tres y es la que entra sin control.**
>
> Y la segunda mitad agrava la primera: el título **obliga al sistema a ocultarle a Rosa el origen de
> la solicitud**. `RF-105` le da la última palabra sobre el cambio de punto y `RF-187` le quita el
> único dato con el que la ejercería. Un impostor que pasa por Elena obtiene, **bajo el nombre del
> propio sistema**, una oferta de redirección presentada a Rosa como iniciativa de SendIt.

No es una redacción para mejorar: es la decisión de qué se le dice a la dueña del dinero cuando la
solicitud no es suya, y hoy está decidida en contra suya **sin que ningún ítem lo declare como
decisión**.

## D4 — coherencia · 1,66

```
n = 13 · d = 13/192 = 0,0677 · D = 0,40 · D4 = 2 × (1 − 0,0677/0,40) = 1,66
```

| Clase | n |
|---|---:|
| Ambigüedad no marcada | 5 |
| Decisión sin requerimiento | 4 |
| Título no atómico | 3 |
| Duplicado encubierto | 1 |
| **Huérfano** | **0** — `RF-104` y `RF-143` reasignados a Kevin |
| **Título no entendible** | **0** — `RF-162` reescrito |
| Tensión esquivada · Prioridad ausente | 0 |

**Dos clases enteras cayeron a cero por primera vez en la serie.** Gradiente verificado por conteo
directo y coincide exacto con la tabla del propio backlog: RF `146/21/7`, RNF `15/3/0`.

**Se anota sin deducir, porque la lista de clases es cerrada:** `RF-181` no existe y **no figura en
«Identificadores retirados»**. Es el único hueco de numeración no declarado en trece rondas.

## Las siete tensiones — cero sin decidir

T1 a T7 decididas, tercera ronda consecutiva. **T2 quedó cerrada por los dos estados esta ronda**
—`RF-40`+`RF-170` callan el motivo, `RF-152`+`RF-171` impiden deducirlo desde la acción— y es *la
única de las ocho correcciones aplicada en `R` exactamente como se pidió*. Reservas anotadas: T1
tiene acotado uno de los tres plazos de `RNF-09` y no los otros dos; T5 viaja a una sola punta; T7
acota los intentos de `RF-179` **por giro y no por quien pregunta**.

## Los cuatro rebarridos

**Cifra sin productor.** *Cerradas:* el plazo de reporte (`RF-185`) y la vigencia de las listas
(`RF-186`). *Abiertas:* **la constancia de que el código llegó** —`RF-149`, `RF-173` y `RF-174`
dependen de ella y el único productor es `RF-131`: **tres títulos nuevos se apoyaron en una cifra
que la misma ronda no produjo**—; el efectivo del agente, abierta desde la ronda 10; el factor `6,5`
avisos por giro, y `E-escalar:451` **advierte por escrito el defecto que vuelve a cometer en el mismo
archivo que lo denuncia**; la vida de la cotización antes de la ventanilla.

**Acto sin consumidor.** *Cerrados:* el *no pagable* llega a alguien (`RF-172`); el motivo de una
retención tiene lector por omisión (`RF-170`) y acción que no lo delata (`RF-171`). *Abiertos:* el
resultado `no_entregado` es terminal y nada lo consume; y el *no pagable* **no llega a Elena**.

**Reparación en la altitud equivocada — sexta ronda consecutiva.** `L:873-877` apoya en `RF-40` la
garantía sobre el reporte a la autoridad, **en un archivo que sí se repropagó entero a 192 filas**;
`RNF-09` recibió techo y consecuencia para uno de sus tres plazos —se corrigió el síntoma y no la
decisión que lo generó: que un identificador lleve tres promesas—; y `D:829`/`D:831` siguen
declarando como supuestos propios dos cifras que `RF-186` y `RF-183` ya subieron.

**Estado sin terminador.** *Cerrados — los tres que la ronda 12 dejó abiertos:* el *no pagable*
(`RF-173`, `RF-174`), el descuento objetado (`RF-184`) y la reposición no retirada (`RF-175`,
`RF-176`). *Abiertos:* el techo de 10 minutos de `RNF-09`; `RF-154` sin plazo; **la carrera
`RF-173`↔`RF-174`, nacida esta ronda**; y el giro que no se puede pagar por caja insuficiente.

## Verificación de los ocho bloques

**Seis cerrados · uno cambiado de forma · uno no aplicado.** El que no se aplicó es el bloque 8, **y
es el único que no costaba un título**: era una restricción sobre cómo escribir los otros siete.

El bloque 4 merece nombre propio: `RF-178`/`RF-179`/`RF-180` cerraron la reserva de Elena exactamente
como la pidió, y `RF-187` —escrito en el mismo bloque para cubrir el otro lado de la misma
dependencia— **cerró 0,5 de D1 y abrió 0,5 de D3**.

## Correcciones para la ronda 14

Escritas en [`../CORRECCIONES-RONDA-14.md`](../CORRECCIONES-RONDA-14.md). **Las tres primeras son un
solo título cada una y valen medio punto cada una.** Con las cuatro primeras aplicadas:
`D1 3,0 · D2 3,0 · D3 2,0 · D4 ≈ 1,7` ≈ **9,7**. Con dos cualesquiera de las tres primeras: **≥ 8,6**.
