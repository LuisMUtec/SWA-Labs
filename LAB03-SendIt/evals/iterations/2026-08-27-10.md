# Iteración 10 — 2026-08-27

Evaluado: [`redale/R-requerimientos/backlog.md`](../../redale/R-requerimientos/backlog.md)
@ `22e0868` — **120 RF + 17 RNF = 137 ítems**.

Rúbrica: [`evals/README.md`](../README.md), con `D4` en su forma enmendada. Nada aterrizó a mitad de la
corrida: los tres agentes de persona y el agregador leyeron el mismo SHA.

## Puntaje

| Dim | Pts | Obtenido | Por qué |
|---|---|---|---|
| D1 Satisfacción de las personas | 3 | **1,5** | Los tres devuelven `Funciona con reservas`. **Primera ronda sin un solo `No funciona`** |
| D2 Ajuste al problema | 3 | **3,0** | Censo **38 de 137**: el 72,3 % del backlog deja de tener sentido en una transferencia doméstica |
| D3 Cobertura de seguridad y consistencia | 2 | **1,5** | Consistencia 1,0 con invariante en los tres puntos de rotura. Seguridad 0,5: el camino de falla queda descubierto |
| D4 Coherencia del backlog | 2 | **0,54** | `n = 40` · `d = 40/137 = 29,20 %` · `2 × (1 − 0,2920/0,40)`. Ningún techo duro ata |
| **Total** | **10** | **6,54** | |

**Gate ≥ 8: NO PASA**, por 1,46. Décima ronda, décima vez.

**Contra la 09 (6,5): +0,04, y el número miente sobre lo que pasó.** D1 subió medio punto —las dos
tensiones que la 09 había reabierto, T2 y T3, están cerradas, y Elena sale de `No funciona`— y D4 bajó
0,46 porque **esta ronda barrió aguas abajo las tres mil líneas de `E`, `D`, `A`, `L` y `E-escalar` que
ninguna anterior había barrido enteras**. Los 40 hallazgos están anclados uno a uno a un identificador
y a una línea; ninguno se descubrió aflojando la vara. La fórmula por densidad mide, entre otras cosas,
**cuánto se miró**.

## Veredictos de persona

### Rosa — `Funciona con reservas` (resta 0,5)

- **Flujo principal:** corre entero. `RF-05` los soles antes de pagar, `RF-63` + `RF-02` el cobro en
  efectivo en Huanta, `RF-06` + `RF-07` el monto fijo, `RF-52` + `RF-53` + `RF-77` el estado,
  `RF-65` + `RF-64` el cierre cuando Elena cobra.
- **Cuando sale mal:** cubierto con las cuatro piezas. Aviso dentro de plazo (`RF-67`), plazo informado
  de antemano y acotado (`RF-115`, `RF-96`), devolución sola al vencer (`RF-32`) en efectivo, en su
  punto y con la comisión adentro (`RF-71`, `RF-72`, `RF-74`), y `RF-43` + `RF-118` para no pagar dos
  veces.
- **Qué me frustra:** `RF-111` le devuelve el giro si no contesta dentro de un plazo que `RF-116` solo
  informa y **nadie acota por abajo** — es la persona que limpia casas y no atiende el teléfono. Y
  entre la retención y la liberación (`RF-33`) **nadie le avisa**: `RF-53` la obliga a consultar y
  `RF-66` solo le muestra que está detenida. `RF-91` calla qué pasa si no hay otro punto en el distrito.
- **Comprobado y no deducido:** no restó por no poder ver el motivo del rechazo — `RF-40` es una
  decisión tomada, no un hueco.

### Elena — `Funciona con reservas` (resta 0,5)

- **Flujo principal:** corre entero por primera vez y cierra en `RF-64`.
- **Cuando sale mal:** el documento vencido sale por el desafío (`RF-86`, `RF-69`, `RF-122`), y la
  diferencia de monto por `RF-90` + `RF-59`.
- **Qué me frustra:** `RF-124` y `RF-108` **no nombran canal** — *«un aviso que no dice cómo llega, para
  mí no llega»*—, contra los catorce ítems vecinos que sí lo nombran. `RF-105` no cambia el punto sin
  la respuesta del emisor y `RF-111` devuelve el giro si no llega: la informan de un lugar al que no
  puede ir. No existe el simétrico de `RF-52`: **solo puede recibir avisos empujados, nunca preguntar.**
  Y `RF-98` guarda que el aviso se mandó, no que ella lo recibió.
- **Comprobado y no deducido:** no restó por `RF-70` — que no se pueda cambiar al receptor la protege.

### Kevin — `Funciona con reservas` (resta 0,5)

- **Flujo principal:** **se corta en el paso 4.** `RF-79` le cierra la operación y ahí se queda:
  `RF-113` le quita el código de la pantalla —lo prefiere—, `RF-11` dice que el sistema lo entrega
  «únicamente al emisor» sin decir por qué canal, y la fila `T5` afirma lo contrario, que no lo
  transmite a nadie. **Ningún título dice qué le entrega a la señora que estira la mano.**
- **Cuando sale mal:** aquí sí está todo. `RF-118` deja el efectivo cobrado sin giro en estado legible
  y retomable, `RF-42`, `RF-43` y `RF-44` hacen del reintento la misma operación, y `RNF-05` en
  formulación fuerte vuelve el doble pago **imposible, no improbable**.
- **Qué me frustra:** `RF-61` exige segundo rol para *corregir* el monto de un giro pagado pero no le
  impide **iniciarla** sobre uno pagado en otra ventanilla (`RNF-02` solo cubre el mismo giro).
  `RF-92` y `RF-95` hablan de «pago» mientras `RF-71`, `RF-72` y `RF-32` le hacen entregar efectivo en
  **devoluciones**: devuelve sin control de caja. `RF-08` y `RF-09` dicen «antes de registrar» y él
  registra **después** de recibir (`RF-79`). Y `RF-59` + `RF-90` + `RF-104` convierten la palabra
  telefónica de un receptor que ya se fue en un descuento sobre la caja de su patrón, con `RF-104` en
  P1 y todo lo que gasta ese efectivo en P0.
- **Comprobado y no deducido:** no restó por `RF-40` ni por `RNF-09`; el techo de 10 minutos por el
  segundo rol es una decisión declarada.

`D1 = 3 − 0,5 − 0,5 − 0,5 = **1,5**`

## D2 — censo del ajuste al problema · 3,0

| Rasgo | Ítems que se vuelven falsos si el rasgo desaparece | Pts |
|---|---|---|
| Cruza una frontera | RF-15, RF-16, RF-17, RF-18, RF-19, RF-22, RF-23, RF-31, RNF-12 | **1** |
| Cruza una moneda | RF-05, **RF-06** (queda literalmente vacío: no hay nada que recalcular), RF-07, RF-21, RF-88, RF-117, RNF-14 | **1** |
| Termina en efectivo, sin banco, en manos de un tercero | RF-02, RF-34, RF-63, RF-71, RF-76, RF-92, RF-95, RF-102, RF-114, RF-24, RF-104, RF-85, RF-86, RF-122 | **1** |

Ítems que sobreviven intactos a la desaparición de los tres rasgos: **38 de 137 (27,7 %)**, y son casi
todos cumplimiento genérico, idempotencia y privilegio. **99 ítems (72,3 %) dejan de tener sentido.**

Serie del censo: `4/41` → `41/81` → `51/97` → `73/110` → `78/118` → **`99/137`**.

## D3 — seguridad y consistencia · 1,5

**Consistencia 1,0.** Los tres puntos donde de verdad se rompe llevan invariante declarable, no
porcentaje: `RNF-06` (nunca retenido y disponible a la vez), `RNF-05` (ninguna doble entrega, «en
ningún punto y en ningún momento»), `RNF-13` (nunca devuelto y cobrado). Camino de falla cubierto por
`RF-42`, `RF-118`, `RF-43`, `RF-44` y `RNF-08`.

**Seguridad 0,5 — el camino de falla queda descubierto**, y es exactamente el modo que el rol tiene
mandato de buscar:

> `RF-90` → `RF-59` → `RF-104` convierte la palabra telefónica de un receptor que ya salió del local en
> un **descuento sobre la caja del patrón de Kevin**. Ningún título exige identificar a quien llama
> como el receptor designado —`RF-85`, `RF-86` y `RF-114` solo corren en el mostrador—, ninguno exige
> comprobación antes de la reposición, y `RF-104`, lo único parecido a un control, es **P1** mientras
> `RF-59`, que gasta el efectivo, es **P0**. **Informar no es autorizar.**

El barrido lo confirma aguas abajo: `D:272-274` cierra explícitamente toda entrada originada por el
receptor y `L:695` lista el operador de voz con **16 avisos salientes y cero entradas**. El canal que
`RF-90` concede no existe en ninguna parte; el descuento que dispara, sí.

Grieta menor del mismo tipo: `RF-61` no impide **iniciar** la corrección sobre un giro pagado en otra
ventanilla, porque `RNF-02` solo cubre la cadena *de un mismo giro*.

## D4 — coherencia · 0,54

```
n  = 40
d  = 40 / 137 = 0,2920
D  = 0,40
D4 = 2 × (1 − 0,2920/0,40) = 0,54
```

**Techos duros:** una sola tensión sin decidir (T5) — el techo exige dos, **no ata**. Gradiente de
prioridad real, 114 P0 / 18 P1 / 5 P2 — **no ata**. Sin unanimidad conforme — **no ata**.

### Los 40 hallazgos por clase

| Clase | n | Identificadores |
|---|---:|---|
| Tensión esquivada | 1 | **T5** |
| Decisión sin requerimiento | 10 | RF-17 · RF-01/RF-80 · RNF-15 · RF-76/RF-95 · RF-05 · RF-104 · RF-44/RNF-05 · RF-120/RF-121 · RNF-09 · RF-99 |
| Duplicado encubierto | 4 | RF-45 · RF-58 · RF-107 · RF-21 |
| Título no entendible | 1 | RF-108 |
| Título no atómico | 4 | RF-39 · RF-98 · RF-115 · RF-117 |
| Ambigüedad no marcada | 20 | RF-08 · RF-09 · RF-17/RF-22 · RF-47 · RF-59 · RF-60 · RF-78 · RF-82 · RF-89 · RF-91 · RF-92 · RF-93 · RF-95 · RF-96 · RF-99 · RF-110 · RF-123 · RF-124 · RNF-15 · RNF-17 |
| Prioridad ausente | 0 | Ninguno — pero **la asignación está invertida en tres pares**: RF-104 (P1) controla a RF-59 (P0); RF-24 (P1) liquida lo que RF-95 (P0) gasta; RF-58 (P1) cuenta lo que RF-93 (P0) castiga. Anotado, no cobrado |

### El hallazgo que ordena a los demás — T5, la tensión de los canales que no decide su propio canal

La fila `T5` dice que el código «el sistema **no transmite a nadie**». `RF-11` dice que lo **entregará**
al emisor. `RF-113` dice que lo **hará llegar** al emisor. El invariante de `RNF-15` dice que el sistema
«puede comprobar un código sin poder mostrarlo». **Cuatro textos, tres decisiones incompatibles, y
ningún título nombra el canal — en un backlog que escribe «llamada o mensaje de texto» en catorce
ítems.** La tensión que trata *de canales* es la única que no decidió el suyo, y es la misma pared
contra la que Kevin choca en su paso 4.

## Las siete tensiones

| # | Decidida | Dónde |
|---|---|---|
| T1 Control ↔ rapidez | Sí, con reserva | RF-26 · RF-119 · RF-31 · RF-32 · RNF-09. La reserva: RF-08 y RF-09 aún dicen «antes de registrar» |
| T2 Explicación ↔ reserva | **Sí — restituida esta ronda** | RF-39 · RF-40 · RF-66 · RF-67 · RF-115 |
| T3 Identidad del receptor | **Sí — cerrada esta ronda con RF-122** | RF-86 · RF-84 · RF-68 · RF-69 · RF-122 · RF-101 · RF-12 · RF-114 · RF-82 |
| T4 Certeza del monto ↔ exposición | Sí | RF-05 · RF-06 · RF-07 · RNF-16 |
| **T5 El canal de Rosa ↔ el de Elena** | **NO** | La única sin decidir. Ver arriba |
| T6 Cerrar ↔ cerrar de verdad | Sí, con reserva | RF-64 · RF-46. La reserva: RF-110 vuelve a abrir el límite |
| T7 El código perdido | Sí | RF-94 · RNF-15 · RF-110 · RF-117 · RF-123 |

## Los cuatro rebarridos de propagación

Registrados los cuatro. **Ninguno vino vacío**, y es la primera ronda que los corre sobre los cinco
pasos enteros.

### Cifra sin productor — 8

Umbral de reporte transfronterizo (RF-17, RF-22 · `E-estimar:165` inventa USD 10 000) · cuánto más
corto es el plazo de la homonimia (RF-78) · número de intentos del desafío (RF-82 · `E-estimar:176` le
atribuye 3) · longitud de la ventana de acumulado (RF-09, RF-58, RF-93 · `E-estimar:175` la fija en 30
días) · **la comisión** (RF-74 la devuelve, RF-117 no la recobra, y nadie la fija ni obliga a
informársela a Rosa) · el margen cambiario (`A:121`, `D:464`) · el plazo de liquidación con el agente
(RF-24) · el máximo de una revisión (RF-96, contra un supuesto que declara «sin techo global»).

*Correctamente respaldadas y por tanto no hallazgo:* RNF-09/10/11, RNF-12/RNF-14, el piso de
conservación, la prescripción y las 72 h. **El defecto «productor sin consumidor» de la iteración 02
sigue cerrado.**

### Acto sin consumidor — 5

El motivo de liberación de `RF-87` se registra y nadie está obligado a leerlo — **y nada le avisa a
Rosa que su retención se levantó** · el aviso `no_entregado` de `A:138` es terminal y no reintentable,
visible para ningún componente · **el canal telefónico de `RF-90` no existe aguas abajo**: `D:272-274`
cierra toda entrada del receptor y `L:695` lista 16 avisos salientes y cero entradas · la pregunta del
desafío de `RF-69` no figura en el enum de diecisiete tipos de aviso, contra `RF-98` · `diferencia_cierre`
se calcula, se guarda, y no avisa, no abre caso ni entra en la liquidación.

### Reparación en la altitud equivocada — 7

**Tercera ronda consecutiva con la primera**: `RF-13` se corrigió a «antes de que el operario reciba» y
`RF-08` y `RF-09` se quedaron en «antes de registrar» — se reparó el ítem donde se vio el síntoma, no
la regla que lo gobierna · **`RF-124` nació esta ronda para cerrar un hallazgo y nació sin canal**, que
es el defecto que las rondas anteriores ya habían cobrado en RF-25, RF-37 y RF-41: la reparación
reprodujo la clase que venía a reparar · la fila `T5` no se tocó al agregar `RF-113` · `L:622` sigue
transcribiendo el título de `RF-104` de la ronda 09 en una tabla que declara copiarlos «tal como están
escritos allá», y `L:470`/`L:471` **ejecutan** esa versión · `L:572` cierra `DONE` la versión vieja de
`RF-39` · `E-estimar:174` corrigió la atribución de RF-60 y la celda de al lado sigue diciendo lo viejo
· **`RF-02` sigue produciendo la deriva semántica que el propio backlog advierte**: `E` lo usa seis
veces con el sentido retirado.

### Estado sin terminador — 4

La custodia de `RF-118`: `A:119` declara **«no hay transición por vencimiento»** y el pasivo queda
abierto para siempre · la devolución no retirada de `RF-89` al vencer la prescripción — `E-estimar:439`
la cifra en 11,5 % del almacenamiento caliente · **`pago en duda`** nace en `D:427`, no existe en A ni
en L y ningún título de R lo enuncia · la retención nacida de `RF-93` tiene `caso_id` nulo y todo el
aparato de liberación cuelga del caso de cumplimiento.

*Cerrado y confirmado:* la retención de sanciones sí tiene terminador (RF-31 + RF-32). El hallazgo de
la iteración 01 sigue saldado.

## Correcciones para la ronda 11

Cuarenta ediciones de título, agrupadas por el puntaje que liberan. Se aplican **después** de esta
corrida y se miden en un SHA nuevo.

| Bloque | Qué libera | Qué toca |
|---|---|---|
| **1 — el canal del código** | El hallazgo de tensión, medio punto de D1 y el paso 4 de Kevin | `RF-11` gana canal y absorbe a `RF-113`; la fila `T5` deja de decir «a nadie»; el invariante de `RNF-15` se acota a su título |
| **2 — el camino del descuento** | +0,5 en D3 | Un título que identifique al declarante de `RF-90`; `RF-59` exige comprobación; `RF-104` sube a P0 y vuelve el aviso **condición** del descuento; `RNF-02` se extiende más allá de «un mismo giro» |
| **3 — las tres reservas** | D1 a 3,0 | Canal en `RF-124` y `RF-108`; «pago **o devolución**» en `RF-92` y `RF-95`; «antes de recibir» en `RF-08` y `RF-09`; aviso de liberación; simétrico de `RF-52` para Elena; piso al plazo de `RF-116`; salida de `RF-91`; reidentificación en `RF-123`; `RF-110` alineado con `RF-46` |
| **4 — forma** | Nueve hallazgos baratos | Partir `RF-39`, `RF-98`, `RF-115`, `RF-117`; retirar `RF-45`, `RF-58`, `RF-107`, `RF-21`; corregir la columna Persona de `RF-60` y `RF-99`; marcar las cinco cifras huérfanas; fijar la comisión; terminador para `RF-89` y `RF-118` |
| **5 — aguas abajo** | Las diez decisiones sin requerimiento | O se escribe el título, o se saca la decisión de `E`, `D`, `A`, `L` |

**Techo alcanzable en la 11**, con los bloques aplicados: `D1 3,0 · D2 3,0 · D3 2,0 · D4 ≈ 1,7` ≈ **9,7**.
Lo que separa del gate no es una dimensión: son cuarenta ediciones de título.
