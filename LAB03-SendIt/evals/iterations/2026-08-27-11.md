# Iteración 11 — 2026-08-27

Evaluado: [`redale/R-requerimientos/backlog.md`](../../redale/R-requerimientos/backlog.md)
@ `b33944b` — **138 RF + 17 RNF = 155 ítems**. Propagación aguas abajo @ `97c7f6b`; **el backlog no
cambió entre uno y otro** (verificado: `git diff b33944b 97c7f6b` no toca `R`). Los tres agentes de
persona y el agregador leyeron el mismo SHA.

Rúbrica: [`evals/README.md`](../README.md), con `D4` en su forma enmendada del 2026-08-27.

## Puntaje

| Dim | Pts | Obtenido | Por qué |
|---|---|---|---|
| D1 Satisfacción de las personas | 3 | **1,5** | Los tres devuelven `Funciona con reservas` por segunda ronda consecutiva |
| D2 Ajuste al problema | 3 | **3,0** | Censo **95 de 155**: el 61,3 % deja de tener sentido en una transferencia doméstica |
| D3 Cobertura de seguridad y consistencia | 2 | **1,5** | Consistencia 1,0. Seguridad 0,5: el camino de la ronda 10 **está cerrado de verdad**; hay uno nuevo |
| D4 Coherencia del backlog | 2 | **0,61** | `n = 43` · `d = 43/155 = 27,74 %` · `2 × (1 − 0,2774/0,40)`. Ningún techo duro ata |
| **Total** | **10** | **6,61** | |

**Gate ≥ 8: NO PASA**, por 1,39. Undécima ronda, undécima vez.

**Contra la 10 (6,54): +0,07.** El backlog creció 13 %, cerró **28 de los 40 hallazgos** de la ronda
anterior, decidió la última tensión que quedaba abierta y sacó a las tres personas de todo bloqueo
duro — y el puntaje se movió siete centésimas. La razón no es que el instrumento se haya endurecido:
es que el rebarrido de esta ronda fue tan profundo como el de la 10 y encontró **43 hallazgos nuevos,
todos anclados a un identificador y a una línea**.

## Veredictos de persona

### Rosa — `Funciona con reservas` (resta 0,5)

- **Flujo principal y ramo malo:** corren enteros. Los cuatro ramos de la retención tienen ítem
  nombrado y no supuesto: `RF-133` (el aviso dentro de la hora), `RF-96` (el plazo máximo antes de
  pagar), `RF-32` (la devolución sola al vencer), `RF-43` (no pagar dos veces).
- **Qué la frustra:** `RF-91`, `RF-116` y `RF-111` **no nombran canal**, y sobre ese aviso corre el
  plazo de 24 horas que le devuelve el giro, mientras el backlog nombra el canal en los otros
  diecinueve avisos: *«no es un plazo, es un sorteo»*. Y `RF-135` —no recobrar la comisión del giro
  rehecho— quedó en **P1** mientras `RF-110` y `RF-117` son **P0**: puede existir el rehacer sin la
  exención.
- **Comprobado y no deducido:** no restó por `RF-29` («ni pagable ni cancelable»), que le quita el
  botón durante la retención — está dicho explícitamente y `RF-32` le pone fondo. Tampoco por las
  once cifras con `[ASSUMPTION]` cerradas esta ronda: *«un supuesto declarado no es un título
  ambiguo»*.

### Elena — `Funciona con reservas` (resta 0,5)

- **Flujo principal:** los cinco pasos tienen ítem y **cada aviso nombra su canal**. `RF-64` cierra el
  envío cuando ella recibe el dinero, que era su rechazo más duro; `RF-122` le resuelve el documento
  vencido en el mostrador **sin despertar a Rosa a las cinco de la mañana**; `RF-143` y `RF-142` le
  ponen nombre y responsable a los treinta soles.
- **Qué la frustra:** `RF-34` exige el código y `RF-113` prohíbe transcribirlo, **y no existe para el
  código el equivalente de `RF-114`** — *«el ramo que el backlog resolvió para mi DNI vencido lo dejó
  abierto para el código»*. `RF-128` no nombra canal: *«es el único comodín de canal que me queda, y
  está justo en el ítem que se llama sin depender del emisor»*. Y `RF-84` decidió la **escritura** del
  nombre; **quién decide si se parece a la foto de sus cincuenta años sigue siendo el operario.**
- **Comprobado y no deducido:** buscó activamente un ítem que le dé un poder que no debe tener y no lo
  encontró. *«Ninguno de los 155 me da un poder que no debo tener. Cero deducción por permisos.»*

### Kevin — `Funciona con reservas` (resta 0,5)

- **Flujo principal y ramo malo:** los cuatro ramos que le costaban dinero están decididos con el
  momento nombrado: `RF-13`, `RF-118`, `RF-43`, `RF-44`, `RNF-05`.
- **Qué lo frustra:** **el mismo paso 5 que Elena** — *«la suposición que me piden hacer es de las
  caras: que Elena lo dicta en voz alta y yo lo tecleo, lo que RF-113 prohíbe. Con cola esperando, eso
  lo resuelvo con criterio propio, y mi criterio propio es lo que un defraudador estudia»*. `RF-125`
  le confirma que el código **salió**, no que llegó, y cuando el emisor dice «no me llegó nada» ese
  giro no está rechazado, ni retenido, ni es no pagable: `RF-39` no le asocia acción y `RF-130` no le
  muestra nada. Y la acción ofrecible **puede delatar el motivo que `RF-40` omite**.
- **Comprobado y no deducido:** *«lo busqué activamente y el backlog cierra las puertas por las que yo
  sería el eslabón comprable»* — `RNF-02` extendido le impide hasta **iniciar** la corrección de un
  giro pagado. Sin deducción por permisos.

`D1 = 3 − 0,5 − 0,5 − 0,5 = **1,5**`

## D2 — censo · 3,0

| Rasgo | Ítems que se vuelven falsos | Pts |
|---|---|---|
| Cruza una frontera | RF-04, RF-15…RF-20, RF-22, RF-23, RF-31, RF-139, RF-140, RNF-12 — **13** | **1** |
| Cruza una moneda | RF-05, **RF-06** (*queda literalmente vacío: no hay nada que no recalcular*), RF-07, RF-88, RF-134, RF-142, RNF-14, RNF-16 — **8** | **1** |
| Termina en efectivo, sin banco, en manos de un tercero | 54 ítems, de RF-02 a RNF-17 | **1** |

**95 de 155 (61,3 %) dejan de tener sentido.** Serie: `4/41` → `41/81` → `51/97` → `73/110` →
`78/118` → `99/137` → **`95/155`**.

**Y esto se anota aunque no cueste puntos:** la fracción específica de remesa **cayó 11 puntos**, de
72,3 % a 61,3 %. No se perdió nada transfronterizo — **de los 18 ítems que esta ronda agregó, 11
sobreviven intactos a una transferencia doméstica**. El backlog creció por su mitad genérica. *Si el
patrón se repite dos rondas más, D2 baja sin que nadie haya empeorado un ítem.*

## D3 — seguridad y consistencia · 1,5

### Consistencia · 1,0
`RNF-05` (doble pago), `RNF-06` (retenido y disponible), `RNF-13` (devuelto y cobrado), `RNF-07`,
`RNF-16`. Camino de falla con `RF-42`, `RF-118`, `RF-138`, `RF-43`, `RF-44`, `RF-100` y los dos
nuevos **`RF-144`** y **`RF-146`**.

### Seguridad · 0,5 — el camino viejo cerrado, uno nuevo abierto

**El camino que la ronda 10 nombró está cerrado de verdad, y se dice sin descuento:**

| Pedido en la ronda 10 | Estado |
|---|---|
| Identificar al declarante de `RF-90` | **`RF-126`, P0.** La palabra telefónica ya no basta |
| `RF-104` a P0 y el aviso como condición | **Hecho, y mejor que lo pedido**: pasó de un deber de informar a una **prohibición negable** |
| `RNF-02` más allá de un mismo giro | **Hecho.** La grieta de `RF-61` está cerrada |

**El medio punto es por uno nuevo, y es la clase que el rol tiene mandato de buscar por su nombre:**

> `RF-34` **exige el código para autorizar el pago**. `RF-113` y `RNF-15` **prohíben al mostrador
> leerlo o transcribirlo**. Y **no existe para el código el equivalente de `RF-114`**. El control que
> autoriza **todos** los pagos del sistema no tiene enunciada ninguna forma de ejercerse, y el único
> camino implementable con los títulos que hay es que Elena le dicte el código a Kevin — que es
> precisamente lo que `RF-113` prohíbe.

Aguas abajo la ausencia **ya se llenó inventando**: `D:813` afirma que el secreto *«entra y sale por
un dispositivo orientado al cliente»* citando `RF-114`, que solo cubre el desafío; y `L:548` describe
el `Terminal del Cliente` como *«donde el emisor lee su código»*, **que contradice a `RF-11`**, el
título que esta misma ronda escribió.

**Es una reparación en la altitud equivocada aplicada a la corrección de la ronda anterior.** La
ronda 10 vio el síntoma —Kevin no tiene qué darle a la señora que estira la mano— y reparó la
**salida**. No reparó la decisión que lo gobierna: *por dónde se mueve el secreto en un mostrador
donde el operario no puede tocarlo*, que tiene dos extremos y solo uno se decidió.

Grietas residuales: `RF-59` cambió una ambigüedad por otra («comprobada», sin decir quién comprueba),
y **la inversión de prioridad se movió un eslabón en vez de eliminarse** — se corrigió `RF-104` y la
ronda creó `RF-145` (P1) por debajo de `RF-143` (P0).

## D4 — coherencia · 0,61

```
n = 43 · d = 43/155 = 0,2774 · D = 0,40 · D4 = 2 × (1 − 0,2774/0,40) = 0,61
```

**Techos duros: ninguno ata.** Tensiones sin decidir **0** — por primera vez desde la ronda 05 las
siete están decididas con ítem nombrado. Gradiente 129 P0 / 21 P1 / 5 P2, ningún ítem sin prioridad.
Sin conformidad unánime.

| Clase | n |
|---|---:|
| Decisión sin requerimiento | 14 |
| Ambigüedad no marcada | 15 |
| Duplicado encubierto | 5 |
| Título no entendible | 4 |
| Título no atómico | 3 |
| Huérfano | 2 |
| Tensión esquivada · Prioridad ausente | 0 |

**Las tres inversiones de prioridad se anotan y no se cobran**, por la misma regla con que la ronda 10
las anotó sin cobrarlas — la vara no se endurece a mitad de serie: `RF-145` P1 controla a `RF-143` P0;
`RF-135` P1 bajo `RF-110`/`RF-117` P0; `RF-142` P1 bajo `RF-136` P0.

### Los cinco hallazgos que la ronda se hizo a sí misma

Vale nombrarlos aparte, porque son **correcciones de la ronda 11 que produjeron el defecto que venían
a corregir** — cuarta ronda consecutiva con la clase:

1. **`RF-98` se partió por atomicidad y en el corte perdió su consumidor.** La versión anterior decía
   *«y la opondrá a quien alegue no haber sido avisado»*. Hoy la constancia se guarda y nadie la opone.
2. **`RF-128` nació sin canal** — exactamente la clase que la ronda 10 cobró en `RF-124` y `RF-108`, y
   que esta ronda **sí** cerró en esos dos. La reparación reprodujo la clase por tercera ronda.
3. **`RF-110` se alineó con `RF-46` y se volvió su duplicado literal**, con el mismo borde palabra por
   palabra.
4. **`RF-47` ganó «comisión incluida», cerró su ambigüedad y abrió el solape con `RF-74`.**
5. **`RNF-15` se extendió al mostrador y su gemelo `RNF-17` no.** Se corrigió el invariante donde se
   vio el síntoma, no el par.

## Las siete tensiones — cero sin decidir

| # | Estado |
|---|---|
| T1 Control ↔ rapidez | **Decidida; la reserva de la ronda 10 quedó cerrada** — `RF-08`, `RF-09` y `RF-13` dicen los tres «antes de que el operario reciba el efectivo» |
| T2 Explicación ↔ reserva | Decidida, con reserva: la acción ofrecible puede delatar el motivo que `RF-40` omite |
| T3 Identidad del receptor | Decidida, con reserva: `RF-84` decidió el eje **escritura**, `RF-122` el eje **vigencia**, y el eje **la foto no la representa** sigue en manos del operario |
| T4 Certeza del monto ↔ exposición | Decidida — y `RF-134` la extiende al giro rehecho |
| T5 El canal de Rosa ↔ el de Elena | **Decidida esta ronda**, con reserva: cuatro ítems sin canal y **la entrada del código sin decidir** |
| T6 Cerrar ↔ cerrar de verdad | **Decidida; la reserva de la ronda 10 quedó cerrada** — al precio de un duplicado |
| T7 El código perdido | Decidida, con reserva: `RF-135` en P1 bajo `RF-110`/`RF-117` en P0 |

## Los cuatro rebarridos

**Cifra sin productor.** Las **diez cifras que esta ronda cerró se consumen bien** — verificadas una
por una en `E`, `D`, `A` y `L`, sin una sola divergencia numérica. **Es el rebarrido más limpio de la
serie.** Sigue sin productor: la **comisión** (cuatro lectores, ninguno la fija), el **margen** —`A`
celebra que dejó de ser «campo sin lector» y no advierte que sigue sin quien lo produzca— y el
**umbral del acumulado del receptor**.

**Acto sin consumidor.** El motivo de liberación de `RF-87` sigue sin lector obligado; la constancia
de `RF-98` perdió el suyo al partirse; y **`A` declara cinco cuentas contables donde `D` ejecuta
ocho**, de modo que la retención de `RF-29` y toda devolución no se pueden asentar. *Cerrados y
confirmados:* `diferencia_cierre` ya tiene destinatario, `pago en duda` ya está enunciado,
`Retencion.caso_id` ya es `NOT NULL`, la pregunta del desafío ya está en el enum de 22 avisos.

**Reparación en la altitud equivocada.** Los cinco casos de arriba, más `RF-02` con su sentido
retirado en `A`, `D` y `L` —**cerrado en `E`, donde ya no aparece ni una vez**—, y una que merece
nombre propio: **`E-escalar:60` reinterpreta el título de `RNF-12`** para que la configuración quepa.
Eso es corregir en la dirección prohibida: los requerimientos bajan, nunca suben.

**Estado sin terminador.** Los **cuatro** que la ronda 10 nombró están cerrados en `R`: la custodia
(`RF-138`), la devolución no retirada (`RF-137`), `pago en duda` (`RF-144`/`RF-146`) y la retención de
`RF-93` (`caso_id NOT NULL`). Quedan cinco nuevos, y el más limpio de enunciar: **la devolución que
nace de la prescripción nace vencida**, porque `RF-89` la mantiene disponible «hasta el plazo de
prescripción del giro» y `RF-50` la crea **al prescribir**.

## Verificación de los 40 hallazgos de la ronda 10

**28 cerrados · 6 cambiaron de forma · 6 abiertos.** El detalle fila por fila está en el informe del
agregador. Los dos que siguen abiertos sin haberse tocado: **`RNF-17`**, cuyo gemelo se extendió al
mostrador y él no, y **el umbral del acumulado de `RF-93`**.

Y el que sí se saldó por completo: **ninguna transcripción vieja de título sobrevive en la tabla de
trazabilidad de `L`** — las 155 filas verificadas palabra por palabra, ninguna sobra, ninguna falta,
y no queda ningún puntero vivo a los ocho identificadores retirados. `L` dejó de transcribir los
títulos y pasó a **extraerlos** del backlog, que es lo que mata la clase de raíz.

## Correcciones para la ronda 12

Escritas en [`../CORRECCIONES-RONDA-12.md`](../CORRECCIONES-RONDA-12.md). El techo alcanzable con los
tres primeros bloques aplicados es `D1 3,0 · D2 3,0 · D3 2,0 · D4 ≈ 1,0` ≈ **9,0**; con los ocho,
≈ **9,7**.

**Lo que separa del gate ya no es una dimensión: son D1 y D4 a la vez, y los dos primeros bloques
mueven las dos.** Dieciséis ediciones de título valen 1,5 puntos de D1 y 0,5 de D3.
