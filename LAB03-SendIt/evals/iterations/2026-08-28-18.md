# Iteración 18 — 2026-08-28

Evaluado: [`redale/R-requerimientos/backlog.md`](../../redale/R-requerimientos/backlog.md)
@ `96164c2` — **207 RF + 20 RNF = 227 ítems**.

| Dim | Pts | Obtenido | Por qué |
|---|---|---|---|
| D1 | 3 | **1,50** | Los tres `Funciona con reservas`, novena ronda consecutiva |
| D2 | 3 | **3,00** | Censo bruto **128 de 227 (56,4 %)**; sustantivo **86 (37,9 %)** |
| D3 | 2 | **1,50** | Consistencia 1,0. **Seguridad 0,5 — baja desde 1,0**: `RF-202` contra `RF-94` |
| D4 | 2 | **1,6696** | `n = 15` · `d = 15/227 = 6,61 %`. Ningún techo duro ata |
| **Total** | **10** | **7,6696** | **Gate ≥ 8: NO ALCANZADO**, por 0,3304 |

**Serie: 7,73 · 7,70 · 8,00 · 7,67. El cruce de la ronda 17 no se sostuvo.**

## Qué lo revirtió, dicho sin adornos

**Un título que esta ronda escribió para cerrar un hallazgo y que abrió uno mayor.**

`RF-202` se amplió a *«el aviso … que el destinatario dice no haber recibido»* para alcanzar el
silencio del canal, que era la reserva de Elena. Pero **el código de seguimiento viaja como aviso** —
la propia tabla de supuestos lo dice al escribir *«el silencio del canal vale como entrega para todo
aviso **salvo el que lleva el código**»*—, de modo que `RF-202` **reemite el código a quien llame y
diga que no le llegó**, y ningún título identifica a quien reclama un reenvío: `RF-205` identifica al
que consulta, `RF-197` al que comprueba, **ninguno al que reclama**.

`RF-94` (P0) dice *«no volverá a entregar un código ya emitido, ni siquiera al emisor que lo
perdió»*. **Dos P0 que no pueden regir a la vez sobre el mismo aviso.**

**Lo encontró Rosa, contra su propia conveniencia:** *«lo reporto aunque un reenvío me convendría»*.
Es el «nunca debe» de su archivo, y bajo la regla de D1 le habilitaba un `No funciona`; devolvió
`Funciona con reservas`. El agregador deduce **0,5 y no 1,0** en D3 porque la exposición está acotada
—`RF-11` fija el destino en el teléfono que el emisor registró, así que quien llama dispara el
reenvío pero no lo recibe—, y cita el precedente: en la **ronda 06**, tres ítems que negaban el
invariante que los justificaba costaron **un punto entero**.

**Es la quinceava recurrencia del patrón que la ronda 04 nombró:** *entra la corrección y trae
consigo un defecto de la clase que vino a corregir*.

## Los diez bloques — nueve limpios, uno catastrófico

| Cerró limpio | Abrió algo |
|---|---|
| `RF-105` decide en su altitud el caso de `RF-211` — **corta la racha de diez rondas de *reparación en la altitud equivocada* en esa pata** | **`RF-202`** — el de arriba |
| `RF-144` + `RF-221`: Kevin cerró sus dos reservas contra estos dos ítems | `RF-168` acredita el monto y **no dice cuándo se firma** — dos lecturas, y `RF-214` la usa de techo |
| `RF-223` termina `RF-219`: éxito, fracaso, plazo y vencimiento | `RF-222` **choca con `RF-83`** — la reposición se reclama después de que `RF-64` cerró el giro, y bajo una lectura la respuesta ya caducó |
| `RF-162` partido · `RF-218` dice contra qué identifica · `[CLARIFY]` en `RF-147`/`RF-114` · cuatro subidas a P0 | `RF-225` abre **una cuarta espera con la persona delante que ningún plazo acota**, y `RNF-09` afirma «la suma de los tres» |
| `insumos.md` con un solo aviso, verificado | `RF-226` **no atómico** — irónico en la ronda que partió `RF-162` por atomicidad |
| **Integridad de identificadores verificada exacta, por primera vez sin resto**: cada número de `RF-01` a `RF-226` aparece una vez, vivo o declarado muerto | **La fila T6 quedó afirmando el borde de cancelación que `RF-46` abandonó** — la corrección entró sin barrer la decisión que la gobierna, dentro del propio backlog |

Y **la autocertificación de la disciplina de alcance falla por tercera vez en cinco rondas**: declara
6/6 en la 17 y 6/6 en la 18, y bajo la regla escrita sobreviven seis títulos. A `RF-224` le atribuye
*«el canal de voz»*, palabras que su título no tiene. *«Tres autocertificaciones falsas en cinco
rondas dejan de ser un error de conteo y pasan a ser una propiedad del documento.»*

## La falla de instrumento, registrada y no enmendada

El agregador responde la pregunta que la ronda 17 dejó abierta: **el cuantum de D4 bajó** —de 0,02252
a 0,02203— *«la fórmula se hace más fina justo cuando la superficie crece y los juicios marginales se
multiplican»*. Y mide el error de criterio con tres reglas:

| Medición | Deriva | Contra un cuantum de |
|---|---|---|
| Dentro de su propia corrida (`n ∈ [15,17]`, dos decisiones defendibles) | **0,044** | 0,022 |
| Contra la ronda 17 (`n = 15` vs `n = 22`, sobre 5 ítems cambiados de 227) | **0,154** | 0,022 |
| El censo de D2, misma patología en otra dimensión | **12 puntos porcentuales** | — |

**De ahí la conclusión, que es el resultado de esta ronda:**

> *«Con D1 congelada en 1,5 por nueve rondas y D2 en 3,0 por once, el gate lo decide íntegramente si
> el agregador encuentra o no un defecto de media unidad en D3. La dimensión de resolución más fina
> —D4— no tiene influencia alguna sobre el veredicto: a `n = 0` el total con esta D3 sería exactamente
> 8,00, y a `n = 22` con la D3 de la 17 habría sido 8,00 igual. **La rúbrica quedó con una sola
> dimensión decisoria y tres descriptivas.**»*

**No la enmienda**, y el motivo es el mismo con que la 17 rechazó la enmienda de D2: *«enmendarlo en
la ronda que lo detecta y que además voltea el veredicto reincidiría en endurecer o ablandar por su
efecto, con el cambio y el resultado entrando juntos»*. La evidencia de la condición 1 queda reunida
para quien quiera tratarlo en su propia ronda.

**Y la consecuencia práctica para leer la serie:** la comparación `D4(17)=1,50 → D4(18)=1,67` **no
significa nada** —está dentro del error—, pero la caída del total **sí es legible**, porque no la
produce D4: es un escalón de media unidad en D3, muy por encima del ruido.

## Correcciones para la ronda 19

**La primera devuelve 0,5 de D3 y con ello el gate:** decidir qué hace el sistema cuando alguien dice
que no le llegó el aviso que lleva el código — **y decidirlo en la altitud donde vive, que es T7 y
`RF-94`, no reescribiendo `RF-202`**. Las otras nueve suman 0,33 de D4.
