# Iteración 14 — 2026-08-27

Evaluado: [`redale/R-requerimientos/backlog.md`](../../redale/R-requerimientos/backlog.md)
@ `e8cd465` — **180 RF + 20 RNF = 200 ítems**.

Rúbrica: [`evals/README.md`](../README.md), con `D4` enmendada. **La enmienda candidata a D2 queda
sin aplicar**: la condición 2 del Principio IV exige que corra en su propia ronda, y esta corrigió
ocho bloques.

## Puntaje

| Dim | Pts | Obtenido | Por qué |
|---|---|---|---|
| D1 Satisfacción de las personas | 3 | **1,5** | Los tres `Funciona con reservas`, quinta ronda consecutiva |
| D2 Ajuste al problema | 3 | **3,0** | Censo **99 de 200 (49,5 %)**: frontera 16 · moneda 8 · efectivo/tercero 75 |
| D3 Cobertura de seguridad y consistencia | 2 | **1,5** | Consistencia 1,0. Seguridad 0,5: `RF-90` quedó sin compuerta al retirarse `RF-126` |
| D4 Coherencia del backlog | 2 | **1,75** | `n = 10` · `d = 10/200 = 5,00 %` · `2 × (1 − 0,05/0,40)`. Ningún techo duro ata |
| **Total** | **10** | **7,75** | |

**Gate ≥ 8: NO PASA**, por 0,25. Decimocuarta ronda, decimocuarta vez.

**Contra la 13 (7,66): +0,09, y el movimiento entero vuelve a ser D4.** `D1`, `D2` y `D3` no se
movieron por segunda ronda consecutiva. `n` cayó de 13 a **10** mientras el backlog crecía 4,2 %:
`D4 = 1,75` es la mejor lectura de la serie y le quedan 0,25 puntos de recorrido.

**Lectura alternativa declarada por el agregador:** si se contaran por separado la compuerta de
`RF-90` y el cierre de la superficie del receptor —que la ronda 13 contaba como dos—, `n = 11`,
`D4 = 1,72` y el total sería **7,72**. En ninguna de las dos lecturas se alcanza el gate.

## El hallazgo que ordena la ronda

**Retirar `RF-126` declarándolo «contenido en `RF-180`» fue falso, y el barrido aguas abajo
reemplazó el identificador en vez de comprobar la contención.**

> `RF-90` deja al receptor **declarar una diferencia por llamada o mensaje de texto**. `RF-180`
> identifica a quien **consulta**, **comprueba** o **solicita un cambio de punto**. Declarar no está
> en la lista. Y detrás de la declaración hay dinero: `RF-193` le da cinco días hábiles, **`RF-194`
> la repone cuando el plazo vence sin decidirse**, `RF-155` la paga en efectivo en un punto y
> `RF-143` la descuenta de la liquidación del agente. **Una declaración sin compuerta se convierte en
> un pago por silencio.**

Es el mismo defecto que la ronda 13 cobró en `RF-187` —el acto remoto más consecuente de su familia
entrando sin control— y esta ronda **lo cerró en un ítem y lo abrió en otro**.

**El agravante:** cuatro pasos aguas abajo creen que la compuerta existe —`D:289-291`, `D:328`,
`A:122`, `A:140`, `L:862`, `E-estimar:82`— y `L:880-882` **enumera los tres disparadores de `RF-180`
y lo aplica a un cuarto dentro de la misma oración**. Un hueco que todos creen tapado es peor que uno
visible.

Y la no atomicidad es lo que lo hizo posible: `RF-180` lleva tres disparadores separadamente negables
bajo un identificador, **y eso permitió que el cuarto desapareciera sin que nadie lo viera**.

## Veredictos de persona

### Rosa — `Funciona con reservas` (resta 0,5)

Flujo entero; los cinco pasos y las cuatro exigencias del ramo malo sobre títulos que pasan las tres
pruebas. Reconoce el bloque 1: *«este backlog no usa la obligación de reserva como excusa»* — le
calla el motivo (`RF-170`, `RF-171`) y aun así le da el aviso (`RF-67`, `RF-133`), el plazo dicho de
antemano (`RF-96`) y la devolución sola (`RF-32`). Cero deducción por permisos, comprobados pieza
por pieza.

**Reserva:** `RF-174` devuelve el giro a las 72 h sin constancia, **y la salida barata no alcanza a
ese estado.** `RF-117` dice «el giro **cancelado** por código perdido», y un giro **devuelto** por
`RF-174` no lo es salvo suponiéndolo: queda pagando comisión de nuevo (`RF-135` no aplica) y tomando
la cotización del día (`RF-134` no aplica). *«Es exactamente la cuenta que me quemó la vez pasada.»*

**Anota sin restar:** `RF-63` la deja elegir el punto y nadie le dice **antes de soltar el efectivo**
si ese punto puede pagar; `RF-52` es la única consulta sin canal nombrado; y **la medida de `RNF-20`
cita `RF-192` para decir qué pasa al vencer los 10 minutos, cuando quien lo dice es `RF-191`**.

### Elena — `Funciona con reservas` (resta 0,5)

Flujo entero, y **por primera vez llega al paso 5 sin tener que suponer nada**: `RF-64` cierra cuando
recibe el dinero y comprobó que ningún ítem lo contradice. Cero deducción por permisos; miró `RF-187`
con lupa — *«pido, no dispongo»*.

**Reserva:** `RF-25` le dice que hay un giro y `RF-41` cuánto, **y ningún ítem le dice dónde.**
`RF-75` pregunta por «el punto elegido» presuponiendo que ella ya sabe cuál es. Y el argumento se
apoya en el propio backlog: **escribe «y en qué punto» tres veces** —`RF-124`, `RF-156`, `RF-14`— y
no lo escribe en el aviso del cobro original. *«Ese hueco es exactamente un viaje de cuarenta minutos
de ida, la cola y cuarenta de vuelta.»*

### Kevin — `Funciona con reservas` (resta 0,5)

Flujo entero, los cuatro ramos cerrados en formulación fuerte. **Su reserva de la ronda 13 está
cerrada:** cita `RNF-20` y `RF-191` como el final que faltaba. Cero deducción por permisos, buscada
activamente.

**Reservas, tres, todas con ID:** **(a) `RF-184` y `RF-194` hacen que el incumplimiento de plazo de
*SendIt* se cobre de la caja de su patrón** — los relojes de `RF-153` y `RF-193` vencen los dos
contra la misma caja; **(b) `RF-144` es el único de los tres estados de interrupción que no dice «que
el operario puede leer»**, y es justo el del pago, donde el dinero ya salió; **(c) `RF-125` le
confirma que el código *salió* mientras `RF-149` decide con *llegó*** y `RF-189` resuelve 15 minutos
después, con `RNF-09` fijándole 3 minutos de atención: Rosa ya se fue, `RF-172` le avisa a ella,
`RF-190` al receptor, y a él ningún título.

`D1 = 3 − 0,5 − 0,5 − 0,5 = **1,5**`

## D2 — censo · 3,0

| Rasgo | Ítems | Pts |
|---|---|---|
| Cruza una frontera | 16, con `RF-193` nuevo | **1** |
| Cruza una moneda | 8. **Ninguno de los nueve nuevos lo toca, segunda ronda consecutiva** — es el 4,0 % del backlog y no crece desde hace veinticinco ítems | **1** |
| Termina en efectivo, sin banco, en manos de un tercero | 75, con `RF-191`, `RF-192`, `RF-195`, `RNF-20` nuevos | **1** |

**99 de 200 (49,5 %)**, con el censo **enumerado entero** y la comprobación publicada: 99 + 42
(sobreviven intactos) + 59 (sobreviven y sirven) = 200. ✓

**La caída se detuvo en el borde.** Contra el titular de la ronda 13 (53,1 %) baja 3,6 puntos;
contra su desglose auditable (49,0 %) **sube 0,5 y es la primera vez en cuatro rondas que no cae**.

**Nota de auditoría que corresponde registrar:** la ronda 13 declaró su censo «enumerado ID por ID»
y su informe **no lo enumera** — publica 15 · 8 · 71 = **94** bajo un titular de **102**. Los dos
números no cierran entre sí. El de esta ronda va enumerado para que la 15 pueda restarlo del suyo.

**La disciplina de alcance funcionó donde se aplicó: la mortalidad de títulos nuevos subió de 33 % a
55,6 %.** Y su autocertificación es falsa: el bloque cuenta **diez** títulos y hay **nueve**, y
afirma que mueren los diez cuando **cuatro sobreviven** — `RF-189`, `RF-190`, `RF-194` y `RNF-19`.
La razón escrita para `RNF-19` —«porque la devolución es efectivo en un mostrador»— no está ni en el
título ni en el invariante.

## D3 — seguridad y consistencia · 1,5

### Consistencia · 1,0

Cero adjetivos. **Siete** invariantes declarables, y **`RNF-19` cierra el candidato a deducción que
la ronda 13 dejó anotado**: la carrera `RF-173`↔`RF-174`. `RNF-13` prohibía *devuelto y cobrado* y no
*devuelto y pagable*; ahora las dos están prohibidas. **Es la corrección hecha en la altitud
correcta.**

El agregador **no deduce** por la contradicción `RF-189`↔`RF-132`: es entre dos reglas funcionales,
no un invariante ausente. Se cobra entera en D4.

### Seguridad · 0,5

Lo que está bien y hay que decirlo: el backlog decide **quién no puede hacer qué** con nombre y
circunstancia, no con algoritmos —`RNF-02`, `RNF-18`, `RF-33`, `RF-61`, `RF-13`+`RF-191`, `RF-57`,
`RF-70`, `RF-105`, `RF-48`, `RF-113`/`RF-147`/`RF-114` con `RNF-15`/`RNF-17` detrás—. **Los tres
agentes buscaron activamente una concesión de permisos y ninguno la encontró.**

**El bloque 4 se aplicó entero y bien** —`RF-180` extendido cubre `RF-187`, `RF-192` le devuelve a
Rosa el origen— **y no cobra su medio punto, porque el mismo movimiento destapó el disparador que
quedó fuera**: el hallazgo que ordena la ronda, arriba.

## D4 — coherencia · 1,75

```
n = 10 · d = 10/200 = 0,0500 · D = 0,40 · D4 = 2 × (1 − 0,05/0,40) = 1,75
```

| Clase | n |
|---|---:|
| Ambigüedad no marcada | 4 |
| Decisión sin requerimiento | 4 |
| Título no atómico | 1 |
| Título no entendible | 1 |
| **Huérfano · Duplicado encubierto · Tensión esquivada · Prioridad ausente** | **0** |

**Cuatro clases en cero.** Gradiente verificado por conteo directo, coincide exacto con la tabla del
backlog: RF `152/21/7`, RNF `17/3/0`.

La *referencia portante rota* de `RNF-20` hacia `RF-192` **la reportaron las tres personas por
separado**. Es la única corrección del pliego pedida por unanimidad.

## Las siete tensiones — cero sin decidir

Cuarta ronda consecutiva. **T5 quedó con el estado *no pagable* viajando a las dos puntas** (`RF-172`
+ `RF-190`); reserva: el punto donde cobrar nunca viaja a Elena. **T7 tiene un borde abierto:** la
salida de `RF-117` nombra el giro *cancelado* y el que `RF-174` **devuelve** no lo es — no cuenta
como esquivada, hay ítem que decide, pero la decisión no cubre el estado que la propia ronda creó.

## Los cuatro rebarridos

**Cifra sin productor.** *Cerradas:* la constancia del código (`RF-189`) y el plazo del segundo rol
(`RNF-20`+`RF-191`). *Abiertas:* el monto que `RF-194` repone sin haberse comprobado —nace esta
ronda—; el efectivo del agente, abierta desde la ronda 10; la vida de la cotización; el factor de
avisos por giro.

**Acto sin consumidor.** *Cerrados:* el *no pagable* llega a las dos puntas, el origen de la
solicitud llega a Rosa, la evidencia del descuento llega al agente. *Abiertos:* **a Kevin no le llega
nada del giro que él creó**; el punto donde cobrar no le llega a Elena.

**Reparación en la altitud equivocada — séptima ronda consecutiva.** El caso de manual: **`RF-126` se
retiró declarándolo contenido, y el barrido reemplazó el identificador en vez de comprobar la
contención.** Segunda: `E-estimar:82` conserva la redacción de `RF-180` que el bloque 7 eliminó por
circular. Tercera: el bloque 3 se aplicó **en la altitud correcta** —se partió `RNF-09`, no se parchó
el síntoma— **y aun así el hijo nuevo nació con la referencia apuntando al ítem equivocado**.

**Estado sin terminador.** *Cerrados:* el techo de 10 minutos, la diferencia declarada, la carrera
`RF-173`↔`RF-174`. *Abiertos:* el giro devuelto por `RF-174` sin ruta de vuelta; el giro que no se
puede pagar por caja insuficiente. **Rebarrido explícito de la retención, que es donde el defecto
vive por definición: no está abierta** — `RF-31` la hace nacer con plazo y `RF-32` la termina. Se
anota el rebarrido vacío.

## Verificación de los ocho bloques

**Cinco cerrados · uno cerrado sin cobrar · dos a medias.** El bloque 4 se cerró entero y no cobró.
El 6 quedó aplicado a medias con autocertificación falsa. El 7 cerró 4 de 7 —**los cuatro herederos
de «no puede pagar» que el propio bloque nombró siguen sin desambiguar**— y el 8 cerró 1 de 5.

## Propagación

**Solo `L` se repropagó.** `D`, `E-estimar` y `E-escalar` siguen declarando **155 ítems**, el cierre
de la ronda 11 — tres rondas y 45 ítems atrás. Búsqueda de los nueve identificadores nuevos en todo
`redale/`: **26 ocurrencias, todas en `backlog.md` y en `L`. Cero en `D`, `A`, `E-estimar` y
`E-escalar`.**

## Correcciones para la ronda 15

En [`../CORRECCIONES-RONDA-15.md`](../CORRECCIONES-RONDA-15.md).

**La primera cruza el gate sola: `D1 1,5 · D2 3,0 · D3 2,0 · D4 ≈ 1,80` = 8,30.** Y por primera vez
en catorce rondas la corrección que separa del gate **es de D3 y no de D1**.
