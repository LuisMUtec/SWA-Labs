# Iteración 19 — 2026-08-28

Evaluado: [`redale/R-requerimientos/backlog.md`](../../redale/R-requerimientos/backlog.md)
@ `dbe94d2` — **216 RF + 20 RNF = 236 ítems**.

| Dim | Pts | Obtenido | Por qué |
|---|---|---|---|
| D1 | 3 | **1,50** | Los tres `Funciona con reservas`, décima ronda consecutiva |
| D2 | 3 | **3,00** | Censo **145 de 236 brutos (61,44 %)**; **116 sustantivos (49,15 %)**, con la comprobación publicada |
| D3 | 2 | **2,00** | **Vuelve a 2,0.** La brecha de la ronda 18 cerrada en su altitud |
| D4 | 2 | **1,7669** | `n = 11` · `d = 11/236 = 4,66 %`. Ningún techo duro ata |
| **Total** | **10** | **8,2669** | |

# **Gate ≥ 8: ALCANZADO**, por 0,2669.

## Lo primero, porque en la ronda 17 no se podía decir

**El margen es de 12,6 cuantums de D4.** A 236 ítems cada hallazgo vale 0,02119: *«para que el
veredicto se cayera por D4 yo tendría que haber omitido **trece** hallazgos. Es la primera ronda de la
serie en que el resultado no depende de un juicio marginal de D4.»*

Contra el 8,0045 de la ronda 17 —que el propio agregador declaró dentro de la resolución del
instrumento, a **un** hallazgo de perderse— esto es un cruce de otra naturaleza.

## El bloque que decidió la ronda, hecho en la altitud pedida

La ronda 18 perdió el gate porque `RF-202` reemitía el código. El pliego pidió decidirlo **en `RF-94`
y T7, no reescribiendo `RF-202`**. Se hizo exactamente eso:

- `RF-202` volvió a su alcance original — el aviso que el canal reporta como no entregado.
- **`RF-227`** escribe el redespacho a pedido del destinatario **identificado**.
- **`RF-228`** excluye ese alcance de forma **incondicional** —«por ningún motivo»—, y su redacción
  absoluta cubre también a `RF-202`, no solo a `RF-227`.

*«Verificado sin contradicción contra `RF-94`, `RNF-15`, `RF-217`, `RF-149`, `RF-174`, `RF-117`. Es
el "salvo" que la 18 no escribió, escrito en la altitud correcta.»*

## `RF-46`: por qué no se cobró en D3

Rosa lo reportó como ruptura de permisos, **contra su propio interés**. El agregador **no lo cobra en
seguridad**, y el argumento corrige el de quien corrigió:

> *«Rosa mueve **su propio dinero** — el backlog declara que "sigue siendo suyo hasta que alguien lo
> cobra". El permiso está compuertado por tres lados (`RF-48`, `RF-206`, `RF-226`), el estado final
> malo está prohibido por invariante (`RNF-13`, `RNF-19`) y Elena recibe el aviso. **Ninguna
> invariante enunciada queda falsa y nadie mueve dinero ajeno: no es un agujero de autorización, es
> una decisión de política — y es la política que el documento declara.**»*

**Lo que sí está roto, y se cobra en D4:** el párrafo de T6 afirma que el borde se corrió *«para que
Rosa no pueda anular con Elena en la ventanilla»* y el borde nuevo produce lo contrario. **Undécima
ronda consecutiva de *reparación en la altitud equivocada*, y la primera en que la variante entra en
la tabla de tensiones**, que es la que gobierna las decisiones.

## Los once bloques — nueve limpios

Cerrados sin residuo: `RF-230` (rama negativa de `RF-84`), `RF-168` («después de contar»), `RF-83`
(«al prescribir»), `RF-226`/`RF-233` partidos, `RF-132` con constancia negativa, `RF-234`/`RF-235`
—que bajan *cifra sin productor* **de 3 abiertas a 1**—, y **`RNF-09` mejor de lo pedido**: dejó de
enumerar tres esperas y acota *cualquiera* con la persona delante, de modo que una quinta no puede
colarse sin techo.

**Y la autocertificación de «Disciplina de alcance» es, por primera vez en la serie, verificada
exacta:** el conteo de la ronda 19 coincide **9 de 9** con el censo del agregador y el de la 18
**6 de 6**. Discrepa en uno del de la 17 y **no lo cobra**, porque la regla escrita dice que en duda
sobrevive.

## D4 · 1,7669 · `n = 11`

**Siete clases en cero**, con el barrido declarado: huérfano, decisión sin requerimiento, comodín,
tensión esquivada, prioridad ausente, **acto sin consumidor** y **estado sin terminador**.

**Integridad de identificadores: exacta, sin resto, por segunda ronda.** `216 + 19 = 235` = el máximo
asignado, con el recorrido por bloques de cincuenta publicado.

**Se anota sin deducir:** el **89,41 %** de los ítems es P0, contra 86,0 % en la ronda 17. *«Un
backlog con nueve de cada diez ítems en P0 está cada vez más cerca de dejar de estar priorizado.»*

## Los cuatro rebarridos

**Acto sin consumidor: 0 abiertas.** **Estado sin terminador: 0 abiertas**, y la retención rebarrida
por **sexta ronda con resultado limpio**. **Cifra sin productor: 1**, contra 3 en la ronda 17.
**Reparación en la altitud equivocada: 2** — undécima ronda consecutiva.

## La pregunta respondida con aritmética, no con aliento

Se le pidió al agregador que dijera si hay algo que una ronda 20 pueda hacer que no sea apostar a que
no aparezca otro defecto de media unidad en D3. Respondió con el cálculo:

> Con `D1 = 1,5` y `D2 = 3,0`, el gate exige `D3 + D4 ≥ 3,5`. Si `D3 = 1,5`, exige `n = 0` **exacto**
> — nunca ocurrió en doce rondas y no va a ocurrir. Si `D3 = 2,0`, exige `n ≤ 23` — se cumple casi
> siempre.
>
> **«El gate se alcanza si y solo si `D3 = 2,0`. D4 no lo decide, no lo ha decidido nunca, y a
> `n = 11` está a trece hallazgos de importar.»**

**Lo único que rompe esa aritmética es `D1`**, el único punto y medio no explotado — y su evidencia
en contra es la más dura de la serie: *«en nueve rondas consecutivas se cerraron las reservas
nombradas y `D1` leyó 1,5 nueve veces. `D1 = 2,0` está disponible en teoría y lleva **0 de 9** en la
práctica.»* Esta ronda repite el patrón en su forma más pura: **Kevin declaró cerradas sus tres
reservas de la 18 y trajo tres nuevas.**

**Y la única acción metodológica que este proyecto nunca intentó:**

> *«Congelar el tamaño. `n` osciló entre 10 y 23 mientras los ítems pasaban de 155 a 236. **El conteo
> absoluto de hallazgos es hoy el mismo que hace siete rondas; `D4` subió porque creció el
> denominador.** La enmienda de la ronda 08 sacó a `D4` de medir tamaño hacia abajo y la puso a medir
> tamaño hacia arriba. Una ronda que no agregue un solo título sería la primera cuyo movimiento de
> `D4` significara algo, y la única condición bajo la cual las reservas de las personas pueden
> agotarse en vez de renovarse.»*

**El veredicto sobre el instrumento, sin adornos:** *«La rúbrica produce un bit por ronda. El aparato
dio lo que podía dar en `D2` y en `D4`. Donde no dio lo que podía es en `D1`, y `D1` no depende del
aparato sino del backlog. No queda margen en el instrumento, queda margen en el documento, y está en
seis títulos concretos.»*
