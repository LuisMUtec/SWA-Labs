# Iteración 12 — 2026-08-27

Evaluado: [`redale/R-requerimientos/backlog.md`](../../redale/R-requerimientos/backlog.md)
@ `ed909ed` — **157 RF + 18 RNF = 175 ítems**. Los tres agentes de persona y el agregador leyeron
el mismo SHA.

Rúbrica: [`evals/README.md`](../README.md), con `D4` en su forma enmendada del 2026-08-27.

## Puntaje

| Dim | Pts | Obtenido | Por qué |
|---|---|---|---|
| D1 Satisfacción de las personas | 3 | **1,5** | Los tres devuelven `Funciona con reservas` por tercera ronda consecutiva |
| D2 Ajuste al problema | 3 | **3,0** | Censo **96 de 175 (54,9 %)**: frontera 14 · moneda 8 · efectivo/tercero 74 |
| D3 Cobertura de seguridad y consistencia | 2 | **1,5** | Consistencia 1,0. Seguridad 0,5: el camino de la ronda 11 **está cerrado**; hay uno nuevo |
| D4 Coherencia del backlog | 2 | **1,34** | `n = 23` · `d = 23/175 = 13,14 %` · `2 × (1 − 0,1314/0,40)`. Ningún techo duro ata |
| **Total** | **10** | **7,34** | |

**Gate ≥ 8: NO PASA**, por 0,66. Duodécima ronda, duodécima vez.

**Contra la 11 (6,61): +0,73 — el salto más grande de la serie.** Las once rondas anteriores
sumaron entre las doce +3,84 puntos; esta sola aporta el 19 % de eso. La razón no es que la vara
se haya ablandado: es que **`n` cayó de 43 a 23** mientras el backlog crecía 13 %, de modo que la
densidad se partió casi por la mitad — de 27,74 % a 13,14 %.

**Y por primera vez en doce rondas el gate está a un solo bloque de distancia, y ese bloque son
cuatro títulos.** Con las correcciones 1 a 4 de la lista de abajo: ≈ **9,5**. Con las 1 a 3
solamente: ≈ **8,8**.

## Veredictos de persona

### Rosa — `Funciona con reservas` (resta 0,5)

- **Flujo principal y ramo malo:** corren enteros. La retención está cubierta en sus cuatro puntos:
  `RF-133` (aviso dentro de la hora), `RF-96` (techo antes de pagar), `RF-32` (devolución sola al
  vencer), `RF-43` (no pagar dos veces). *«Me callan el motivo sin usar esa mordaza para callarme el
  plazo.»*
- **Qué la frustra:** **`RF-149` crea un estado *no pagable* del que ningún ítem le avisa.** `RF-67`
  avisa el paso a *retenido*, y «no pagable» no es «retenido» — el backlog los separa en `RF-29` y
  `RF-49`. `RF-53` solo distingue disponible de todavía-no-disponible. La acción ofrecible de
  `RF-151` se la muestra `RF-130` **al operario**. Y el estado no tiene plazo: el único reloj es la
  prescripción de `RF-49`/`RF-50`. *«Es el mismo silencio de mis seis días, entrando por otra
  puerta.»* También `RF-137`: la devolución prescribe a los doce meses sin aviso previo, mientras el
  giro sí lo tiene en `RF-51`.
- **Comprobado y no deducido:** verificó sus seis «nunca debe» uno por uno —`RF-70`, `RF-94`,
  `RF-101`, `RF-81`, `RF-85`, y el motivo de la retención— y **ninguno está roto**. Tampoco restó
  porque `RF-52` no nombre canal ni por la ausencia de aplicación: *«yo no origino desde una
  aplicación»*.

### Elena — `Funciona con reservas` (resta 0,5)

- **Flujo principal:** los cinco pasos y los cuatro ramos tienen ítem. **`RF-147` cerró la entrada
  del código**, que era su bloqueo de la ronda 11; **`RF-154` le saca al punto la decisión de si su
  diferencia queda comprobada** —*«un rol ajeno al punto, no el bodeguero»*—; `RF-148` cerró el eje
  de la fotografía; `RF-155` y `RF-156` le pagan y le avisan la reposición.
- **Qué la frustra:** **ningún ítem le deja comprobar antes de viajar que el código que Rosa le
  dictó es el correcto.** `RF-128` le da estado, no código. El error se descubre en la cola: un
  viaje, pasaje y medio día. Y `RF-105`/`RF-111`/`RF-164` dejan su cobro a merced de que Rosa
  conteste, sin ítem que la deje pedir u objetar el cambio de punto.
- **Comprobado y no deducido:** declara explícitamente que **no** deduce porque el sistema no le
  mande el código — *«mandármelo lo dejaría de ser secreto»*. Cero deducción por permisos: revisó
  sus cinco «nunca debe» y ninguno está roto.

### Kevin — `Funciona con reservas` (resta 0,5)

- **Flujo principal y ramo malo:** corren enteros, con el momento nombrado en los seis ramos que le
  arruinan el día: `RF-118`, `RF-43`, `RF-44`, `RNF-05`, `RF-13`, `RF-10`.
- **Qué lo frustra:** **(a) `RF-40` protege el motivo del *rechazo* y deja descubierto el de la
  *retención* que `RF-29` crea.** El backlog mismo separa los tres estados en `RF-39`, `RF-150` y
  `RF-151`, y `RF-152` protege solo el motivo «de rechazo» — de modo que el único control sobre el
  motivo de una retención es que Kevin se calle, *«un control que depende de mi honestidad, que es
  precisamente el que mi puesto no aguanta»*. Contrasta con `RF-163`, que sí nombra a quién se le
  muestra el motivo de una liberación: **el backlog sabe hacer esta distinción y acá no la hizo.**
  **(b) `RNF-09` remite la espera por lista externa a `RNF-11`**, que mide disponibilidad
  (99,9 % mensual) y no tiempo: es la única espera que ocurre con la persona delante y quedó sin
  techo.
- **Comprobado y no deducido:** no restó porque el backlog le quite la entrega del código
  —*«es otra cosa que la que yo describí, pero es mejor para mí»*—, ni por `RF-143`, que le cuesta
  caja pero tiene debido proceso en `RF-104`, `RF-145` y `RF-153`. Y certifica sin sumar:
  `RF-84`, `RF-148` y `RF-154` *«le quitan a mi criterio propio las tres decisiones que más me
  podían costar»*.

`D1 = 3 − 0,5 − 0,5 − 0,5 = **1,5**`

## D2 — censo · 3,0

Criterio declarado antes del conteo: un ítem se cuenta cuando **sus propias palabras** nombran el
rasgo. Cada ítem se asigna a **un solo** rasgo, para que la suma no se infle.

| Rasgo | Ítems que se vuelven falsos | Pts |
|---|---|---|
| Cruza una frontera | RF-04, RF-15…RF-20, RF-22, RF-23, RF-31, RF-139, **RF-159**, RNF-10, RNF-12 — **14** | **1** |
| Cruza una moneda | RF-05, RF-06, RF-07, RF-88, RF-134, RF-142, **RF-158**, RNF-14 — **8** | **1** |
| Termina en efectivo, sin banco, en manos de un tercero | **74** ítems, de RF-01 a RNF-18 | **1** |

**96 de 175 (54,9 %).** Serie: `4/41` → `41/81` → `51/97` → `73/110` → `78/118` → `99/137` →
`95/155` → **`96/175`**.

**La fracción específica de remesa BAJÓ otra vez: de 61,3 % a 54,9 %, −6,4 puntos, segunda ronda
consecutiva de dilución.** El aviso que la ronda 11 dejó por escrito se cumplió una de las dos
veces que hacían falta para costar puntos.

La aritmética: el backlog creció **+20 netos** y el censo creció **+1**. De los **24 ítems nuevos**,
solo **13** mueren en una transferencia doméstica y **11 sobreviven intactos** — `RF-150`, `RF-151`,
`RF-152`, `RF-157`, `RF-160`, `RF-161`, `RF-162`, `RF-163`, `RF-165`, `RF-167`, `RF-169`.

**El umbral de la rúbrica no se cruzó** —54,9 % sigue siendo mayoría que muere—, pero con una ronda
más al mismo ritmo cae por debajo de la mitad y **D2 deja de valer 3 sin que nadie haya empeorado un
ítem**.

## D3 — seguridad y consistencia · 1,5

### Consistencia · 1,0

Los seis RNF llevan invariante declarable, no porcentaje. Cero hallazgos de adjetivo. Los tres
lugares que la rúbrica exige están: el pago duplicado (`RNF-05`), retenido y pagado a la vez
(`RNF-06`, `RNF-13`) y la frontera con la red pagadora (`RF-144`, `RF-146`, más `RF-24`, `RF-104`,
`RF-143`, `RF-145`, `RF-153` y `RNF-07`). Camino de falla con `RF-42`, `RF-118`, `RF-138`, `RF-43`,
`RF-44`, `RF-100`, `RNF-08`.

### Seguridad · 0,5 — el camino viejo cerrado, uno nuevo abierto

**El camino que la ronda 11 nombró está cerrado por los dos extremos, y se dice sin descuento:**

| Pedido en la ronda 11 | Estado |
|---|---|
| La entrada del código, que no tenía forma enunciada de ejercerse | **`RF-147`, P0.** El propio receptor lo teclea, sin que quien atiende pueda leerlo ni digitarlo |
| `RNF-17` extendido al mostrador, como su gemelo `RNF-15` | **Hecho.** Los dos lectores quedan nombrados dentro del propio invariante |
| El giro que quedaba mudo cuando el código no llegaba | **`RF-149` + `RF-151`.** Deja de ser mudo — al precio de la reserva de Rosa |

**El medio punto es por uno nuevo, y lo encontró Kevin leyendo solo:**

> `RF-29` crea la retención. `RF-40` omite al operario **el motivo de un rechazo**. `RF-152` obliga a
> que la acción ofrecible **de un rechazo** no permita deducirlo. `RF-150` crea la acción ofrecible
> del giro **retenido** y **ningún título le pone la misma restricción**. El backlog separó los tres
> estados con tres identificadores y protegió uno.

La retención es exactamente el estado que **T2 existe para gobernar**, porque es el que puede tener
un reporte a la autoridad detrás. El sistema puede mostrarle a Kevin por qué está retenido el giro
de Rosa, Kevin es la cara de SendIt frente a ella, y el único control enunciado sobre esa filtración
es que Kevin se calle.

**Es una reparación en la altitud equivocada aplicada a una corrección de esta misma ronda.** Partir
`RF-39` en tres —que era una corrección de atomicidad legítima y pedida— **destapó que la protección
estaba escrita para uno solo de los tres estados**. El defecto no lo introdujo el partido: lo hizo
visible. Estaba tapado dentro de un título no atómico desde que `RF-40` se escribió.

Segundo hallazgo del mismo eje, menor: `L:855-859` sostiene que la existencia de un reporte a la
autoridad no puede filtrarse hacia el cliente por ninguna arista, y lo apoya en `RF-40`, que solo
habla de rechazos.

## D4 — coherencia · 1,34

```
n = 23 · d = 23/175 = 0,1314 · D = 0,40 · D4 = 2 × (1 − 0,1314/0,40) = 1,34
```

**Techos duros: ninguno ata.** Tensiones sin decidir **0**, segunda ronda consecutiva. Gradiente
verificado exacto: RF `132 P0 / 20 P1 / 5 P2`, RNF `15 P0 / 3 P1 / 0 P2`, ningún ítem sin prioridad.
Sin conformidad unánime.

| Clase | n |
|---|---:|
| Decisión sin requerimiento | 8 |
| Ambigüedad no marcada | 8 |
| Título no atómico | 3 |
| Huérfano | 2 |
| Duplicado encubierto | 1 |
| Título no entendible | 1 |
| Tensión esquivada · Prioridad ausente | 0 |

**`n` cayó de 43 a 23 — un 47 % menos — mientras el backlog crecía 13 %.** Es la mejor densidad de
la serie por un margen amplio: 13,14 % contra el 27,74 % de la ronda 11 y el 36,6 % de la 01.

Las **dos inversiones de prioridad** que quedan se anotan y no se cobran, por la misma regla con que
las rondas 10 y 11 las anotaron: `RF-161` P1 bajo `RF-139` P0 y `RF-140` P1 bajo `RF-17` P0 — el
reporte no se puede presentar sin la fecha de nacimiento que él mismo exige. Las tres de la ronda 11
están corregidas.

### Los hallazgos que la ronda se hizo a sí misma

Cuarto año consecutivo de la clase, y esta vez con una vuelta que vale nombrar:

1. **`RF-165` nació duplicado de `RF-93`.** Dar de baja los cobros anteriores a la ventana está
   contenido en «en la ventana», por el mismo criterio con que se retiraron `RF-58` y `RF-112`.
2. **`RF-162` nació comodín.** *«Opondrá la constancia a quien alegue no haber sido avisado»* nombra
   una postura, no un hecho: un sistema que guarda la constancia y nunca la exhibe lo cumple y lo
   incumple a la vez. Se escribió para darle consumidor a `RF-98` y el consumidor quedó nombrado
   mientras el acto no.
3. **`RF-149` abrió un estado sin terminador**, que es la clase en su forma canónica: se abre por la
   ausencia de constancia y nada lo cierra si la constancia llega tarde.
4. **Retirar `RF-108` produjo deriva semántica en tres lugares.** `A:118`, `E-estimar:192` y
   `E-estimar:510` repusieron el comportamiento apuntándolo a **`RF-156`**, el número siguiente que
   quedó libre — que dice otra cosa. Es exactamente lo que la sección «Identificadores retirados»
   del propio backlog existe para impedir, cometido por la corrección que retiró el identificador.
5. **`RF-166`, `RF-167`, `RF-169` y `RF-153` nacieron ambiguos.** Los cuatro se escribieron para
   cerrar comportamientos inventados aguas abajo, y los cuatro dejaron sin marcar la pregunta que
   quedaba: identificación en el mostrador, qué es «vigente», si se puede descartar por otra causa,
   y hábiles de cuál de los dos países.

## Las siete tensiones — cero sin decidir

| # | Estado |
|---|---|
| T1 Control ↔ rapidez | Decidida, con reserva: el precio que T1 prometió acotar —la espera por lista externa— es el único sin techo (`RNF-09` → `RNF-11`) |
| T2 Explicación ↔ reserva | Decidida, con reserva: `RF-40` y `RF-152` cubren solo el *rechazo*; el motivo de la *retención* quedó sin decidir quién puede leerlo. Se cobra en D3 |
| T3 Identidad del receptor | **Decidida entera por primera vez** — vigencia (`RF-86`/`RF-122`), escritura (`RF-84`) y **fotografía (`RF-148`)** |
| T4 Certeza del monto ↔ exposición | Decidida — y el margen ahora tiene productor (`RF-158`) además de lector (`RF-142`) |
| T5 El canal de Rosa ↔ el de Elena | **Decidida por los dos extremos**: salida (`RF-11`) y **entrada (`RF-147`)**. Los cuatro comodines de canal de la ronda 11 nombran su canal. Reserva: el estado *no pagable* que la decisión produjo no viaja por ningún canal |
| T6 Cerrar ↔ cerrar de verdad | Decidida, y **el duplicado que la ronda 11 pagó quedó saldado**: `RF-110` retirado, un solo borde, y `RF-168` conserva la evidencia de que Elena contó los billetes |
| T7 El código perdido | Decidida, y **sin cobrarla dos veces**: `RF-135` a P0, de modo que no puede existir el rehacer sin la exención de comisión |

## Los cuatro rebarridos

**Cifra sin productor.** **El mejor resultado de la serie en esta clase:** las tres que la ronda 11
dejó abiertas están las tres cerradas — comisión (`RF-157`), margen (`RF-158`) y umbral del
acumulado del receptor (`RF-159`), más el plazo de la objeción (`RF-153`) y el de la devolución no
retirada (`RF-89`/`RF-137`). Siguen abiertas cinco: el **plazo para reportar a una autoridad**, la
**vigencia de las listas** de `RF-167` que `D` inventa en 24 h, los **35 KB** del comprobante de
entrega, el factor **`6,5` avisos por giro** —del que depende el primer cuello del sistema y que ya
no corresponde al canal vigente— y el **efectivo del agente**, abierto desde la ronda 10.

**Acto sin consumidor.** *Cerrados:* el motivo de liberación tiene lector obligado (`RF-163`), la
constancia de `RF-98` recuperó su oponente (`RF-162`), `diferencia_cierre` tiene destinatario.
*Abiertos:* el estado *no pagable* se le muestra al operario y no a quien paga ni a quien viaja; el
**motivo de una retención** se produce y ningún título dice quién puede leerlo; el resultado
`no_entregado` es terminal y nada lo consume; y `RF-162` nombra el consumidor sin nombrar el acto,
que es la forma inversa de la clase.

**Reparación en la altitud equivocada.** **Quinta ronda consecutiva**, y los cuatro casos nacen de
correcciones de esta ronda: `D:817` sigue diciendo que el secreto *«entra y sale»* por el
dispositivo del cliente —se corrigió `L:548`, donde se vio el síntoma, y no `D`, que decide la
frontera—; la deriva de `RF-108` a `RF-156` en tres lugares; el descarte de avisos reemplazado por
diferimiento, que tampoco tiene título; y `L:556-564` afirmando 155 ítems mientras `L:576-580`
afirma 175 **en el mismo archivo**.

**Estado sin terminador.** *Cerrados y verificados:* la custodia (`RF-138`), la devolución no
retirada (`RF-89` + `RF-137`), el *pago en duda* (`RF-144`/`RF-146`), la retención de `RF-93`, la
objeción del agente (`RF-153`) y el giro sin respuesta al cambio de punto (`RF-111` + `RF-164`).
*Abiertos:* el ***no pagable* de `RF-149`**, que esta ronda abrió; el **descuento objetado** cuando
el plazo de `RF-153` vence sin resolución; y la **reposición no retirada** de `RF-155`, que es la
devolución de `RF-89` sin su gemelo `RF-137`.

## Verificación de los ocho bloques de la ronda 12

**Seis cerrados · dos parciales.** Los parciales son el bloque 1 —`RF-147` escrito y `L` corregido,
pero `D:817` sin tocar— y el bloque 7, donde los cuatro títulos se escribieron y aguas abajo
quedaron el comprobante digitalizado sin citar `RF-168`, el umbral de 24 h en `D`, y el diferimiento
que reemplazó al descarte.

Y el que se saldó por completo, que era el defecto más grave del rebarrido anterior: **la
contradicción `A`↔`D` sobre las cuentas contables está cerrada.** `A:120` y `D:357-360` declaran las
**mismas siete**, con `obligacion_retenida` y `devoluciones_por_retirar` presentes, de modo que la
retención de `RF-29` y toda devolución ya se pueden asentar y `RNF-07` es comprobable.

## Correcciones para la ronda 13

Escritas en [`../CORRECCIONES-RONDA-13.md`](../CORRECCIONES-RONDA-13.md). El techo alcanzable con
los cuatro primeros es `D1 3,0 · D2 3,0 · D3 2,0 · D4 ≈ 1,5` ≈ **9,5**; con los tres primeros,
≈ **8,8**.

**Lo que separa del gate son cuatro títulos.** Tres de ellos cierran las tres reservas de persona a
la vez que uno de ellos recupera el medio punto de D3 — el motivo de la retención vale 0,5 en D3
**y** 0,5 en D1, que es el caso que la rúbrica anticipa cuando dice que un mismo defecto puede
costar en dos dimensiones y que cobrarlo una sola vez obligaría a elegir cuál de las dos fallas se
perdona.
