# Iteración 15 — 2026-08-28

Evaluado: [`redale/R-requerimientos/backlog.md`](../../redale/R-requerimientos/backlog.md)
@ `19496b7` — **187 RF + 20 RNF = 207 ítems**.

## Puntaje

| Dim | Pts | Obtenido | Por qué |
|---|---|---|---|
| D1 Satisfacción de las personas | 3 | **1,5** | Los tres `Funciona con reservas`, sexta ronda consecutiva |
| D2 Ajuste al problema | 3 | **3,0** | Censo **104 de 207 (50,2 %)**: frontera 17 · moneda 9 · efectivo/tercero 78 |
| D3 Cobertura de seguridad y consistencia | 2 | **1,5** | Consistencia 1,0. Seguridad 0,5: **el efectivo sale hacia el emisor sin compuerta** |
| D4 Coherencia del backlog | 2 | **1,73** | `n = 11` · `d = 11/207 = 5,31 %`. Ningún techo duro ata |
| **Total** | **10** | **7,73** | |

**Gate ≥ 8: NO PASA**, por 0,27. **Contra la ronda 14 (7,75): −0,02 — la segunda regresión de la
serie.**

## El resultado que ordena la ronda, y es sobre el instrumento

**Ocho de los nueve bloques se cerraron enteros, el noveno a medias, y el puntaje bajó.**

El pliego de la ronda 15 afirmaba que su bloque 1 *«cruza el gate solo»*: `D3 → 2,0`, total ≈ 8,30.
El bloque se aplicó **entero y mejor de lo pedido** —`RF-180` partido en cuatro títulos, uno por
disparador, en vez de extendido— y **D3 no se movió**.

> **La corrección no falló. El pliego midió el agujero que había encontrado en vez de la dimensión.**
> La compuerta de `RF-90` era **una** de las dos mitades ausentes de la seguridad. La otra llevaba
> **quince rondas abierta y ninguna la había mirado**.

**Corolario, que vale más que el puntaje de esta ronda: una corrección deducida de un informe corrige
el informe, no la dimensión.** Todo escenario del tipo «con esto solo, ≈ X» es una extrapolación
desde los hallazgos que ya se encontraron, y una dimensión no es la lista de sus hallazgos conocidos.

## La mitad de la seguridad que llevaba quince rondas abierta

La encontraron **Rosa y Kevin por separado, sin verse**:

> El backlog exige identificar contra documento oficial vigente **para recibir** el efectivo
> (`RF-01`) y **para cobrar** el giro (`RF-85`, `RF-86`, `RF-34`, `RF-147`, `RF-114`, `RF-122`,
> `RF-148`, con evidencia firmada en `RF-168`). **Para entregar el efectivo de vuelta no exige
> nada.**
>
> `RF-47`, `RF-32`, `RF-50`, `RF-138`, `RF-174`, `RF-129`, `RF-111` y `RF-164` producen devoluciones
> que `RF-71` paga **en efectivo**, `RF-72` en un punto que el emisor elige y `RF-14` anuncia por
> teléfono. Ningún título dice quién comprueba, en ese mostrador, que quien la retira es el emisor.
> `RF-89` la mantiene disponible doce meses.
>
> Y la superficie remota del **emisor** —consultar, cancelar, elegir dónde cobra la devolución,
> responder un cambio de punto— **no tiene compuerta**, mientras los cuatro disparadores del receptor
> la tienen desde esta misma ronda. **Las dos piezas se encadenan: quien se hace pasar por Rosa
> cancela sin compuerta y cobra sin identificación.**

## Veredictos de persona

- **Rosa — `Funciona con reservas`.** Flujo entero; los seis días de su historia cerrados por
  `RF-133`, `RF-96`, `RF-32`. Firma T2 con estas palabras: *«callar el motivo sin callar el plazo es
  la línea que yo no discuto»*. Cero deducción por permisos. Reserva: **`RF-52` decide *que* consulta
  y no *por dónde***, mientras `RF-128` sí lo decide para Elena.
- **Elena — `Funciona con reservas`.** Flujo entero, y **primera vez que sus cinco pasos tienen un ID
  que los nombra por su hecho y no por su área**. Cero deducción por permisos. Reserva en tres
  partes: **el giro que *nace* no pagable** —`RF-172`/`RF-190`/`RF-200` avisan «cuando **pase** a», y
  un estado inicial no es una transición—; `RF-111` con `RF-105` deja `RF-187` sin efecto propio; y
  `RF-103` con `RF-82` devuelve su problema de identidad a Rosa sin segunda puerta en destino.
- **Kevin — `Funciona con reservas`.** Flujo entero de `RF-01` a `RF-64` sin suponer. Cero deducción
  por permisos. Reserva: **`RF-201` mata la cotización en los mismos 10 minutos que `RNF-20`/`RF-191`
  le conceden al segundo rol** —y `RF-06` no protege nada porque el giro aún no existe (`RF-79`)—; y
  **la salida de efectivo sin compuerta**.

**Las tres reservas del bloque 2 se cerraron y las tres personas devolvieron el mismo veredicto**, por
reservas nuevas. Sexta ronda con `D1 = 1,5` y **primera en que las reservas anteriores están todas
cerradas**: el backlog ya no falla por lo mismo, falla por lo que cada corrección destapa.

## D2 · 3,0 — la fracción sube por primera vez en cuatro rondas

**104 de 207 = 50,2 %**, contra 49,5 % en la ronda 14. Serie: 61,3 · 54,9 · 53,1 · 49,5 · **50,2**.

**El motor es la disciplina de alcance: de los siete títulos nuevos mueren seis — 85,7 %, contra
55,6 % en la ronda 14 — y esta vez la autocertificación del backlog es verdadera**, verificada ID por
ID por el agregador. El bloque 8 quedó cerrado.

`RF-201` es **el primer ítem del rasgo «moneda» en veintiséis ítems de crecimiento**: el rasgo más
delgado del backlog, 9 de 207.

**Nota de auditoría del instrumento:** el censo de la ronda 14 declaró estar «enumerado entero» y
publica tres subtotales sin un solo identificador — el mismo defecto que esa ronda le cobró a la 13.
El de la 15 va enumerado en los tres tramos, con la comprobación `104 + 62 + 41 = 207`.

Y el bloque **«La frontera» del backlog falsifica su propia prueba**: `RF-140` y `RF-161` sobreviven
palabra por palabra a una red doméstica bajo un encabezado que afirma que ninguno lo hace. Es el
mismo hallazgo de la iteración 03, catorce rondas después.

## D3 · 1,5 · D4 · 1,73

`RNF-19` y la generalización de `RF-189` cerraron el bloque 5; los tres lugares de la consistencia
siguen cubiertos y no hay un solo adjetivo. `n = 11`, y **cuatro clases en cero**: huérfano, no
atómico, tensión esquivada, prioridad ausente.

**A esta densidad cada hallazgo vale 0,0242 puntos.** Con `n = 0`, `D4 = 2,0` y el total sería
**8,00 exacto**: D4 sola alcanza el gate, y solo si no queda ni un hallazgo.

## Las siete tensiones — cero sin decidir, quinta ronda

**T7 queda por primera vez sin borde abierto**: `RF-117` dice ahora *«se perdió o no llegó»* y alcanza
al giro devuelto por `RF-174`.

## Los cuatro rebarridos

**Reparación en la altitud equivocada — octava ronda consecutiva, y el caso es el de la ronda
anterior con los identificadores rotados una posición.** La 14 cobró que retirar `RF-126`
declarándolo contenido en `RF-180` fue falso y que **el barrido aguas abajo reemplazó el
identificador en vez de comprobar la contención**. La 15 partió `RF-180` en la altitud correcta —y
**volvió a barrer aguas abajo reemplazando el identificador**: `D:289`/`D:291` atribuyen a `RF-196`
la compuerta de la consulta, que es `RF-180`; `L:819` declara un solo título para tres disparadores
once líneas antes de enumerar correctamente los cuatro.

**Estado sin terminador.** *Rebarrido explícito de la retención: no está abierta* — `RF-31` la hace
nacer con plazo y `RF-32` la termina. Se anota el rebarrido vacío.

## Correcciones para la ronda 16

En [`../CORRECCIONES-RONDA-16.md`](../CORRECCIONES-RONDA-16.md). Dos caminos independientes:
**las compuertas del dinero que sale → 8,26**, o **los once hallazgos de D4 a cero → 8,00 exacto**.
