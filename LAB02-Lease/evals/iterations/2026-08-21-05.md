# Iteration 05 — 2026-08-21

Evaluated: the three specs @ `fa9e44a`.

## Score

| Dim | Pts | Earned | Why |
|---|---|---|---|
| D1 Persona satisfaction | 3 | **2.5** | Pedro and Carlos `Works`; Julia `Works with reservations` |
| D2 Fit to the problem | 3 | **2.5** | BR-13 turns `003`'s defence from a motive argument into a checkable requirement — a rental firm has no balance for a machine to fall below. The body of custody requirements is unchanged, and nothing acts on the answer |
| D3 Demonstrable criteria | 2 | **1.5** | The "signed" schedule defect is completely fixed and all eight compound criteria split; but AC-037 turned on a machine value nothing produced, and atomicity persisted in criteria the split had not reached |
| D4 Coherence and staged scope | 2 | **1.25** | Three propagation survivors |
| **Total** | **10** | **7.75** | |

**Gate ≥ 8:** FAILED — a regression from iteration 04's 8.25, for a reason worth recording.

## Why the score fell

Two of this round's three deductions were **defects introduced by the previous round's corrections**:

- Julia's half point: FR-030 was added to record what a recovery takes, as a capability nobody was obliged to use, so the Recovery meant to save her worst day could open with it empty. She cited the project's own precedent back at it — FR-014b had given departures a producer rather than a place to be written down.
- D3's: the Assessed Value comparison rested on "the machine's value given its condition and hours", a figure no requirement produced and `003`'s own Out of Scope excluded. Structurally identical to the "signed" schedule defect fixed the same round.

Carlos also caught a stale example: FR-012b reasoned from an approval "granted on 30% down" that its own FR-012 forbids under BR-12's 10% cap.

## Persona verdicts

- **Pedro — `Works`.** All eight steps cited; his permissions verified in a table, nine for nine. *"Nothing in my Permissions is unmatched, and nothing I am forbidden is reachable."*
- **Carlos — `Works`.** *"The schedule bends by construction — the instalment waits on the certification — while what is owed never changes."* Residual: the Payer report is warnings-only, so a payer with a clean history looks like one Lea$e has never seen.
- **Julia — `Works with reservations` (−0.5).** One named success signal delivered as an optional capability instead of a guarantee (FR-030).

## To correct before the next iteration

1. Give FR-030 a producer — make it a required handover element.
2. Make the machine's worth constructible rather than assumed.
3. Give collateral adequacy a consequence; a fact wired into no decision is a report, not a rule.
4. The remaining propagation survivors: a false "step 12" reference, a Stage 1 step covered by no User Story, Recovery Close missing from the forward-looking end signal.
