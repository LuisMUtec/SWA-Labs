# Iteration 03 — 2026-08-21

Evaluated: the three specs @ `0b9dfc7`.

## Score

| Dim | Pts | Earned | Why |
|---|---|---|---|
| D1 Persona satisfaction | 3 | **1.5** | Three `Works with reservations` again — but the reservations were markedly smaller and each was concrete enough to close |
| D2 Fit to the problem | 3 | **2.25** | Held. BR-12 now caps a down payment at 10%, closing the gap at the start line as BR-07 closes it at the finish |
| D3 Demonstrable criteria | 2 | **1.25** | Iteration 02's five undefined or load-bearing terms genuinely repaired |
| D4 Coherence and staged scope | 2 | **0.50** | Nine findings, six of one shape |
| **Total** | **10** | **5.5** | |

**Gate ≥ 8:** FAILED

## The finding that mattered

> *The failure mode is now propagation, not substance.*

Six of nine D4 findings were the same shape: a requirement amended correctly while the Key Product Decision, Key Entity, User Story, Success Criterion or Stage 1 step governing it still asserted the old thing. Three separate commits each introduced one.

The aggregator also caught something worse and was right about it: a commit message claimed the `001` Key Entities four-state omission had been fixed, and **the change was not in the file**. A string replacement had failed to match and failed silently. Every correction pass after this one asserts its matches and fails loudly.

Persona findings, all closed: Company could not report its own milestone certification; a mismatch report and a defaulted operation both left the Leasing Operation in a state `001` SC-005 promised did not exist; a guarantee could be set as an approval condition and never recorded as met — a gate with no key; above the Authority Limit an under-evidenced request had no available outcome at all, with two acceptance criteria contradicting each other on that case; a Recovery promised what it takes to retrieve a machine while scope explicitly withdrew recording it; nothing caused a Site Departure to be reported.

## To correct before the next iteration

1. Sweep every governing statement for each amended requirement — assert the matches.
2. Stage the third of the requirement set that belonged to no stage, so Principle V's boundary is applicable.
3. Stop `003` misdescribing its own three ends while defending its fit to the problem.
