# Iteration 06 — 2026-08-21

Evaluated: the three specs @ `dc4520d`. The aggregator also verified which findings the two commits that landed mid-run (`01fd675`, `75d7396`) already close, and reported both readings.

## Score

| Dim | Pts | Earned | Why |
|---|---|---|---|
| D1 Persona satisfaction | 3 | **2.5** | Pedro and Julia `Works`; Carlos `Works with reservations` |
| D2 Fit to the problem | 3 | **2.5** | First round where BR-13 measurably changes the reading: FR-031c is the first requirement in `003` whose *behaviour* differs from a rental firm's, and `002` AC-032c states a risk event with no rental analogue. The custody body underneath is still unchanged |
| D3 Demonstrable criteria | 2 | **1.75** (2.0 at HEAD) | One unverifiable figure — the machinery value, third occurrence of a known class. Closed at HEAD by `75d7396` |
| D4 Coherence and staged scope | 2 | **1.25** | Three findings, two of them the round's own edits failing to reach the User Story that governs them |
| **Total** | **10** | **8.0** (8.25 at HEAD) | |

**Gate ≥ 8:** **PASSED** — 8.0 as scored, 8.25 at HEAD.

**Corrected 2026-08-21, during iteration 07:** this file and the two summary tables first recorded the total as 7.75 and the gate as failed. The four dimension figures were right and their sum was not — 2.5 + 2.5 + 1.75 + 1.25 is 8.0. The arithmetic is fixed here rather than the dimensions, which the aggregator and the persona agents set.

## The finding that repeated

Three separate figures in three consecutive rounds have carried load with nothing producing them: FR-015's *signed* schedule (iteration 04), the Assessed Value (iteration 05), and now the machinery value — which the Authority Limit, BR-12's cap and `002` Stage 1 step 8 all rest on. Carlos deducted for it in D1 and the aggregator in D3, one defect across two dimensions by the rubric's design. Each has been closed by giving the figure a producer rather than by rewording the criterion that used it.

## Persona verdicts

- **Pedro — `Works`.** *"I considered `Works with reservations` on two grounds and rejected both, deliberately rather than charitably."* He also caught `001` AC-006 enumerating four handover elements where `003` FR-003 requires seven — closed by removing the enumeration rather than syncing two lists.
- **Julia — `Works`.** On FR-030: *"that is my success signal word for word — I know where it is and what it takes to get it back instead of starting to find out."*
- **Carlos — `Works with reservations` (−0.5).** The machinery value has no producer, so *"the one bound my file states as absolute rests on a number whose origin the specification does not name."*

## Aggregator findings

| Dim | Finding | Deduction |
|---|---|---|
| D2 | The custody body still survives the gap disappearing: FR-031c re-orders the output of FR-003–FR-014 without changing one of them; impairment reaches two people with no act available against it; and the Assessed Value has no obligation to be current and no staleness signal, unlike hours and location | −0.5 |
| D3 | The machinery value carries the Authority Limit and BR-12's cap and no requirement records it | −0.25 |
| D4 | The Recovery Close edit left AC-018b and AC-018d returning opposite answers for a defaulted Deployment, with US4 sc. 1 and Stage 1 step 9 still on the old model | −0.25 |
| D4 | `003` FR-003 grew to seven elements and User Story 1 did not follow — its Independent Test as written would be refused by FR-003. Aggravator: the client was being asked to accept Lea$e's own valuation, an invented commercial commitment with no marker | −0.25 |
| D4 | FR-031c is exercised by no Stage 1 and absent from the not-exercised list, in a list rewritten by the same commit that added it | −0.25 |

Recorded as checked and not deducted: the Permissions boundary between Carlos and Julia, verified from both sides again; the staged scope's three interlocking seams; and `003` AC-002's pointer to FR-003, where the alternative — a duplicated seven-item list — is the exact mechanism that produced two of the findings above.

## To correct before the next iteration

All five closed immediately after this run: the Assessed Value gains a revaluation obligation at service completion and at any incident, plus a staleness signal (FR-031b, FR-031d); impairment gains an act Underwriter can take that does not move the schedule (`002` FR-030); AC-018b is narrowed to exclude the defaulted case and US4 and Stage 1 follow; `003` User Story 1 is brought into line with FR-003, and the Assessed Value stops being something the client is asked to accept; FR-031c and FR-031d enter the not-exercised list with the empirical hypothesis behind Stage 1's exclusion marked as one.

This file and `2026-08-21-05.md` also close the process gap the aggregator flagged: specs were citing "per EVAL iteration 05" while `evals/iterations/` stopped at 04.
