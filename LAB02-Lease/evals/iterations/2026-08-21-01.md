# Iteration 01 — 2026-08-21

Evaluated (amended 2026-08-21, issue #5 — all three specs, one `main` SHA):

- `specs/001-company-machinery-leasing/spec.md`
- `specs/002-leasing-request-underwriting/spec.md`
- `specs/003-deployed-fleet-custody/spec.md`

@ `c9e3a85`

## Score

| Dim | Pts | Earned | Why |
|---|---|---|---|
| D1 Persona satisfaction | 3 | **1.5** | All three agents returned `Works with reservations` (−0.5 each). Every main flow now runs end to end and every `Must not` is enforced as a denied capability; each persona keeps one stated obstacle unresolved |
| D2 Fit to the problem | 3 | **1.5** | `002` is gap-native. `001` never cites BR-04 and gives an Installment no due condition, so the spec of the actor who *has* the financing problem survives unchanged if the gap disappears; most of `003` would survive it too |
| D3 Demonstrable criteria | 2 | **0.75** | Five criteria cannot be declared met or unmet: a misstated rule, a self-contradicting due condition, and three undefined load-bearing terms |
| D4 Coherence and staged scope | 2 | **0.75** | `001`'s Stage 1 depends on `002` FR-024, which is staged nowhere; `001` FR-015's biconditional contradicts its own four-state model; the two specs describe an Installment differently |
| **Total** | **10** | **4.5** | |

**Gate ≥ 8:** **FAILED**

Per `evals/README.md`: *"A score below the gate does not lower the bar: the spec is corrected and the evaluation is run again."* The corrections are listed under **To correct before the next iteration**; the rubric is unchanged.

## How this iteration ran

Three rounds of persona agents were run, not one. Rounds 1 and 2 surfaced defects that were corrected before the round that produced the verdicts recorded here, so the D1 verdicts below are against `c9e3a85` and not against the state the earlier rounds read. What each round found and what was done about it is recorded in each feature's `checklists/requirements.md`, so the corrections are traceable rather than folded into a single clean number.

The aggregator ran once, against the same SHA, and it is where the score actually broke. That is worth recording plainly: **the persona agents were satisfied by a set the aggregator found unfit for the problem.** Each persona reads its own day and none reads Principle III, which is exactly the division `evals/README.md` §"Who judges what" predicts — *"Each persona sees one angle; none sees the whole problem."*

## Persona verdicts

### Pedro — `Works with reservations` (deducts 0.5)

- **Main flow:** all eight steps run. Need → `001` FR-001/AC-001; request → FR-002/FR-003/AC-002; status → FR-004/AC-003 with the decision itself in `002` FR-011/FR-012; receipt → FR-008/AC-006; instalments with amounts → FR-010/FR-014/AC-008/AC-012; payment → FR-012/AC-010, gated by FR-011/AC-009 (BR-08); acquisition availability → FR-015/AC-013/AC-014, and exercise → FR-017/AC-016, defended from the other side by `003` FR-017/FR-024. Two of three failure branches are handled: rejection (FR-007/AC-005) and machinery mismatch (FR-009b/AC-008b).
- **What frustrates me:** *"pending long enough to threaten the project's timeline"* is detectable only as a state, never as elapsed time — `001` Later stages says so itself. `002` FR-025 covers one cause of the silence (an incomplete file) and not a complete file simply sitting undecided.

### Carlos — `Works with reservations` (deducts 0.5)

- **Main flow:** runs end to end — eligibility `002` FR-004/FR-005/AC-003/AC-004 (BR-02); credit standing FR-006/AC-005; project and schedule FR-007/AC-006; payer FR-008/AC-007/AC-008; decision inside an enforced limit with conditions FR-011/FR-012/FR-013/AC-010–AC-013, escalation FR-022/AC-014, further evidence FR-025/AC-028.
- **The failure case runs too, and structurally:** FR-024 makes certification a fact someone can create; FR-017/AC-020/AC-021 raise the warning to him while the contract is current; because FR-024 makes certification *necessary* for an instalment to fall due, an instalment anchored to an uncertified milestone cannot be overdue — the warning is incapable of arriving after the overdue notice, which inverts his stated life. BR-09 via FR-023/AC-026 settles the tolerance in advance and identically for every operation; FR-020 keeps the declaration his.
- **What frustrates me:** a Certification Milestone now carries its amount (FR-007/AC-006), but no Payer accumulates history across Assessments even though AC-023's own rationale calls a past slip *"evidence for the next decision"*; and speed — the obstacle governing the approvals he is scored on — still has no instrument.

### Julia — `Works with reservations` (deducts 0.5)

- **Main flow:** handover `003` FR-003/FR-004/FR-005/AC-002–AC-004; hours FR-006/FR-008/FR-026/FR-027; service FR-007/FR-009–FR-012/FR-025 (BR-06, BR-10); departure FR-014; close FR-015/FR-017/FR-017b/FR-018/FR-024. Both failure paths resolve: damage at return is an assessment against an immutable baseline (FR-005/FR-015/SC-005), and recovery after a declared default is authorised, informed, and terminable through Recovery Close (FR-018/FR-018b/AC-022b) — without which a recovered machine's Deployment stayed open forever and FR-002 blocked its redeployment.
- **What frustrates me:** step 5 of her own flow. FR-016 no longer lies to her, but it answers *"not yet determined"* for the whole life of every Deployment, because `001` FR-015 withholds the Option until the last instalment and FR-017/FR-019 leave the choice to Company with no deadline. `003` SC-007 concedes it. Her next contract is still planned around a machine she cannot know is coming back.

## Aggregator findings

| Dim | Finding | Where | Deduction |
|---|---|---|---|
| D2 | `001` never anchors payment to project progress. **BR-04 is cited nowhere in `001`**; its Installment carries a status and an amount and no due condition. Strip BR-07 and BR-08 and `001`'s whole journey survives unchanged if the financing gap disappears — verbatim the failure Principle III exists to prevent | `001` Key Entities *Installment*, FR-010–FR-014, US1 sc. 6, Stage 1 step 10 | −1.0 |
| D2 | Most of `003` would survive the gap disappearing: handovers, hours-based service, windows, incidents, departures, safety stops are what any machinery *rental* business needs. Only Acquisition Retirement and Recovery turn on the gap | `003` FR-003–FR-014, FR-019, FR-026, FR-027 | −0.5 |
| D3 | AC-026 restates BR-09's second limb as *"two milestones uncertified at once"*, dropping *"past their expected dates"* — true of every project with two future milestones, so it can never be declared unmet. Also drops "calendar" from 30 days | `002` AC-026 | −0.25 |
| D3 | FR-024 ends *"whichever of the two happens first"* when the due condition is the **later** of certification and receipt — the sentence contradicts the guarantee it just stated | `002` FR-024, AC-027 | −0.25 |
| D3 | *"the operation's contract is still current"* is a load-bearing test condition, and both `001` and `003` record the end of term as unresolved shared scope. Used unmarked | `002` AC-021, SC-005 | −0.25 |
| D3 | *"a cause that is not safety"* — nothing defines what makes a cause a safety cause; BR-10 supplies exactly one instance | `003` AC-024, FR-019 | −0.25 |
| D3 | *"does not match what was approved"* — nothing defines what constitutes a match, and `002`'s Conditions carry only "which machine" | `001` AC-008b, FR-009b | −0.25 |
| D4 | **A Stage 1 depends on a requirement staged nowhere.** `001`'s happy path pays every Installment; under `002` FR-014 each falls due on its milestone's certification, and FR-024 — the only way a milestone becomes certified — is in neither `002`'s Stage 1 nor its later stages. The POC must either never make an instalment due, or pay instalments that never fell due, discarding the BR-04 anchoring that is the point of the system | `001` Stage 1 steps 10–14 ↔ `002` FR-024 | −0.5 |
| D4 | `001` and `002` describe the same Installment differently: `002` makes due-ness a project fact, `001` carries no due condition and lets Company pay any `pending` Installment at will | `001` FR-011/FR-012/Key Entities ↔ `002` FR-014/FR-024 | −0.25 |
| D4 | Internal contradiction: FR-015's *"if and only if … otherwise `not yet available`"* requires `available` after an exercise or decline, while FR-021/AC-020 require exactly one of four states. The four-state model was not carried into FR-015 | `001` FR-015 vs FR-021/AC-020 | −0.25 |
| D4 | `003` Stage 1 step 9 promises the end is knowable *"before the term ends"*; SC-007's own amendment withdrew exactly that claim as undeliverable. Withdrawal applied to the criterion and not to the step it governs. Also: `003`'s Stage 1 admits the Return end, which needs `001` FR-019 (`declined`), which is in no Stage 1 | `003` Stage 1 steps 9–10, SC-007 | −0.25 |

Checked and **not** deducted: the Carlos/Julia deciding-vs-executing boundary is watertight and specified from both sides (`002` FR-021/SC-007, `003` FR-021/FR-023/SC-008, both Authority sections naming the other's requirement); no mechanism or technology appears inside any criterion; nothing is named by its visible text; the `[ASSUMPTION]`/`[CLARIFY]` discipline holds and the three items resolved on 2026-08-21 each resolved *into a catalogued rule* (BR-07, BR-09, BR-10) rather than silently; every FR has at least one AC; the staged scope is real, with deferrals reasoned and cross-referenced rather than dumped.

## To correct before the next iteration

Ordered by what they are worth.

1. **Give `001` BR-04** (≈1.25, fixes D2-1 and D4-2 together). An Installment needs a due condition anchored to a named Certification Milestone, visible to Company, with an FR and an AC that state the effect and cite BR-04. Pedro must be able to see *which* certification each instalment awaits and watch it move when his project moves. This is the single change that turns `001` from a ledger into the thing the brief describes.
2. **Stage a certification event** (0.5, fixes D4-1). Put `002` FR-024 into `002`'s Stage 1, between the approval and `001`'s step 10, so the POC demonstrates one instalment falling due against certified project progress. **This moves Stage 1 and therefore what the POC builds — it must be agreed with Luis before it is merged**, per issue #5's own warning.
3. **Six local repairs** (≈1.0 across D3 and D4): `002` AC-026's second limb; `002` FR-024's *"first"* → *"last"*; `002` AC-021's undefined *"still current"*; `003` AC-024's undefined safety cause; `001` AC-008b/FR-009b's undefined *"match"*; `001` FR-015's biconditional against the four-state model; `003` Stage 1 step 9 against SC-007.
4. Left for a later iteration, recorded so they are not lost: cross-Assessment Payer history (Carlos), an instrument for decision speed (Carlos), elapsed time on a `pending` request (Pedro), and a forward signal of which end a Deployment is heading for before Company acts (Julia).
