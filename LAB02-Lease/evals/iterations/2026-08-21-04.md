# Iteration 04 — 2026-08-21

Evaluated (all three specs, one SHA):

- `specs/001-company-machinery-leasing/spec.md`
- `specs/002-leasing-request-underwriting/spec.md`
- `specs/003-deployed-fleet-custody/spec.md`

@ `c29941c`

## Score

| Dim | Pts | Earned | Why |
|---|---|---|---|
| D1 Persona satisfaction | 3 | **3.0** | All three agents returned `Works`. No deductions — the maximum the asymmetry rule allows |
| D2 Fit to the problem | 3 | **2.5** | The gap is closed at requirement level in three distinct places: the start line (BR-12), the middle (BR-04 anchoring, BR-08 receipt gate) and the finish (BR-07). A body of `003` would still survive the gap disappearing |
| D3 Demonstrable criteria | 2 | **1.5** | Every main flow carries criteria, failure paths included; no unmeasured adjective, no mechanism, no load-bearing citation. Deductions for an unverifiable trigger and for atomicity |
| D4 Coherence and staged scope | 2 | **1.25** | No Permissions contradiction; the staged scope is real and no Stage 1 depends on an unstaged requirement. Three propagation survivors |
| **Total** | **10** | **8.25** | |

**Gate ≥ 8:** **PASSED**

## Persona verdicts

All three `Works` (deduct 0). Each agent was asked to grade in both directions — to deduct for what genuinely fails and *not* to deduct for what their file does not ask for, for reasonably deferred scope, or for wording they would merely have phrased differently.

### Pedro — `Works`

- **Main flow:** all eight steps cited end to end — FR-001/AC-001 → FR-002/FR-003/AC-002 → FR-004/FR-023/AC-003/AC-022 → FR-005/FR-008/AC-006 → FR-010/AC-008 → FR-010b/FR-010c/FR-012/FR-014/AC-012 → FR-015/FR-021/AC-013/AC-014 → FR-017/AC-016/AC-014b, closing against `003` FR-017/AC-021.
- **All three failure modes resolve to a named, observable state** rather than silence: rejection (FR-007/AC-005), a wait he can measure (FR-023/AC-022) and answer (FR-025/AC-024), and a mismatch he can report without being forced to confirm (FR-009b/AC-008b, FR-028/AC-027).
- **Why not a deduction:** the three seams he named — a refusal's reason not reaching him, a committee refusal's route back to his status, certification being recorded by someone else — are outside what his file asks or inside explicitly deferred scope, and in each case he is left with a defined state and a way to see it. *"The standard my success signal sets is: state exactly where my leasing operation stands, without asking anyone. I can."*

### Carlos — `Works`

- **Main flow:** all five steps cited (`002` FR-001–FR-014, FR-022, FR-024–FR-028), with the Certification Schedule now arriving through `001` FR-002 rather than only on request.
- **The failure he lives with cannot form in the shape he described it.** An uncertified milestone leaves its instalment `pending` (`001` FR-010b), so a client not holding money that has not arrived never becomes an overdue account — the schedule bends by construction rather than by anyone's discretion. The tolerance is fixed in advance and identical for every operation (BR-09, FR-023/AC-026/AC-026b), and he declares without ever executing (FR-020/FR-021).
- All four of his success signals are stated as guarantees (FR-016/AC-015/SC-002; FR-014/SC-004; SC-005; FR-009/AC-019/SC-006).
- **Residual, not deducted:** the Payer report is warnings-only, so a payer with a clean history looks like one Lea$e has never seen; no turnaround is committed; BR-12 caps a lever his file calls his. *"One partial instrument and two reasonable deferrals, none of which stops my day from resolving."*

### Julia — `Works`

- **Main flow:** all six steps cited (FR-003/FR-004/FR-005 → FR-006/FR-008/FR-027 → FR-007/FR-009–FR-012/FR-025 → FR-014/FR-014b/FR-029 → FR-016/FR-028 + BR-11 → FR-015/FR-017/FR-018/FR-018b).
- **Her worst day is made unreachable rather than mitigated:** there is no Deployment without a complete, both-sides-accepted Handover Record (FR-003/AC-002), and no receipt confirmation on Company's side that is not that same acceptance (`001` FR-008).
- The recovery she dreaded starts from a declaration that reaches her (`002` FR-020/AC-024) with location, hours, custodian, site and retrieval requirements in hand (FR-022, FR-030), and closes cleanly (FR-018b/AC-022b).
- **Residual, not deducted:** FR-030 permits recording what a retrieval takes without requiring it — she notes it needs a producer the way FR-014b gave one to departures; and FR-010's *agreed* window sits a step short of her file's *require*. Neither stops her day resolving.

## Aggregator findings

The aggregator verified the propagation sweep by grepping for the old claims itself rather than trusting the notes, machine-checked that every FR in all three specs is cited by at least one AC, and confirmed every cross-spec `00n FR-nnn` reference resolves to a requirement that says what the citing sentence claims.

| Dim | Finding | Where | Deduction |
|---|---|---|---|
| D2 | A substantial body of `003` would survive the gap disappearing — handover records, hours readings, Service Due, windows, incidents, departures, inspections, staleness. The spec concedes this and answers with three narrower claims that explain *why Lea$e is doing* generic work rather than making the requirements gap-specific. Principle III's test is on the feature, not the motive | `003` §Problem, FR-003–FR-014, FR-017b, FR-026, FR-027, FR-029 | −0.5 |
| D3 | **Unverifiable trigger.** The immutability guarantee hinges on a schedule being *"signed"*, and no requirement in any spec makes one signed — no event, no state, no Stage 1 step. Nobody can declare AC-018 met or unmet, and it is the criterion carrying Carlos's "must not alter a signed contract's schedule" | `002` AC-018, FR-015, US2 sc. 3, Key Entities, §Authority | −0.25 |
| D3 | **Atomicity.** At least eight criteria assert two or three separately-changeable things under one identifier | `001` AC-008c, AC-012, AC-023, AC-025; `002` AC-021, AC-027b; `003` AC-020b, AC-022b | −0.25 |
| D4 | **Stale cross-reference, false as written.** Both `001` Stage 1 step 11 and `002`'s own exclusion note cite *"step 12"* of `002`'s Stage 1 as the certification step; step 12 is the retrievability step and certification is step 13. The reference broke when a step was inserted. This is the load-bearing claim of the whole set — that `001`'s instalments fall due because `002` certifies — citing the wrong step in both places | `001` Stage 1 step 11; `002` Stage 1 exclusion note | −0.25 |
| D4 | **Stage 1 is not exactly the happy path its User Story describes, while both specs assert it is.** `001` Stage 1 includes settling the approval's conditions (FR-022/FR-024), an act no scenario of US1 covers; `002` Stage 1 step 11 records a Condition met (FR-012c), covered by no scenario of US1 or US2. The Conditions-gating amendment reached the requirements, the criteria and both Stage 1s — not the User Stories governing them | `001` Stage 1 step 9 vs US1; `002` Stage 1 step 11 vs US1–US2 | −0.25 |
| D4 | **Covert duplicate.** Two paragraphs of `003`'s Problem make the same argument in near-identical words — idle machine stops certification, so Lea$e loses the event its repayment depends on. One statement, twice, at the same altitude | `003` §Problem | −0.25 |

Checked and **not** deducted, recorded so the absence is deliberate: the Carlos/Julia deciding-vs-executing boundary is specified from both sides and made unavailable rather than discouraged (`002` FR-021/AC-025/SC-007, `003` FR-021/FR-023/AC-026/SC-008, each Authority section naming the other); every persona's `Must not` lands on a requirement; the apparent friction over Julia's "require a window" is *decided* rather than left open; the staged scope is real, with each spec carrying a Stage 1, an explicit "requirements Stage 1 does not exercise" list, and Later stages; no Stage 1 depends on a requirement staged nowhere — every cross-spec dependency traced; `003`'s five-element handover leaves AC-001/AC-002 narrower but not false, and the fifth element is separately testable through AC-036.

## To correct before the next iteration

Applied immediately after this scoring run, and not yet re-scored — the score above stands at `c29941c`:

1. The two `"step 12"` references, now pointing at `002` Stage 1 step 13.
2. The Conditions beat added to `001` US1 and `002` US1 so Stage 1 and its story match again.
3. The duplicated paragraph in `003`'s Problem, removed.
4. `002` FR-015/AC-018 restated against a state that exists, replacing the undefined "signed".

Left open deliberately: D2's `003` finding is a real property of the feature rather than a defect to patch — custody work resembles rental work, and the spec says so in its own words rather than inflating a defence. Narrowing it further would mean removing requirements Julia's day depends on.
